# Roadmap — the cross-plan index of future additions

**This is the canonical, single-page view of everything proposed-but-not-yet-built**,
gathered from the four detailed plan docs + CLAUDE.md's status bullet. It is a *thin
index*, not a fifth plan: the linked doc + chunk is always the source of truth for
design detail. The value here is the **one cross-plan priority view** no single plan
has — SED + endgame + atlas + stragglers in one place.

When you pick something to build, read its linked chunk; when a plan changes, update
the plan and only the one-line hook here. CLAUDE.md's "what's next" points at this file.

## Status legend

- **done** — shipped (listed only where it's a dependency for unbuilt work).
- **planned** — designed and chunked in a plan doc; ready to implement.
- **sketched** — a concrete approach exists (atlas entry / plan sketch) but not chunked.
- **idea** — named with a rationale; no design yet.

## The cross-plan view (grouped by readiness, not by source doc)

### 1. Ready now — designed, on-hand data, mostly frontend (cheapest honest wins)

| Item | Status | Hook | Where |
|---|---|---|---|
| Instability-strip / variable-class HR overlay | **done** | Shipped: an opt-in toggle on the HR panel shades the classical instability strip (δ Scuti / RR Lyrae / Cepheids, one tilted κ-mechanism band) + LBV and Mira zones as a labeled, schematic *view*. Frontend-only (`hr.js` `setOverlay`). | `whirling-cohort-atlas.md` (bonus) |
| SED Chunk 1 — cool-star coronal X-ray + radio **band** | ✅ done | Shaded `L_X/L_bol` 10⁻⁷…10⁻³ envelope in `sed.js`, frontend-only; **placement is Teff-only** (L_bol cancels via the blackbody effective width 1.521·λ_peak). Güdel–Benz radio = a compact floor marker (it buries on the Fλ axis — kept FLOOR_DECADES=14; the real radio-above-floor payoff is Chunk 2's wind tail). Gated Teff+logg (cool / hot-wind-shock / A-gap / cool-giant). | `magnetic-ember-broadcast.md` §Chunk 1 |
| SED Chunk 3 — band → **line** (age-derived + slider) | ✅ done | Shipped: gyrochronology (Ballesteros→MH08→Wright, all-Wright calibration, Sun→10⁻⁶·²) collapses the band to a cool-blue line for MS cool stars (default-from-age) + a user rotation-period slider (drag-to-override); ±dex fuzz, gated redward of the (B−V)=0.495 singularity, line only in the cool branch. Frontend-only (`sed.js`). | `magnetic-ember-broadcast.md` §Chunk 3 |
| Endgame Chunk 2 — reversible WD gateway + WD mode shell | **done** | Shipped: the "→ Continue: White Dwarf" button at the slider limit, reversible `wd-mode`, 3-zone log-cooling scrub (pulses→central star→cold cinder), live mass/[Fe/H] re-snap, the Teff-coloured WD cooling track on the wide HR axes. Frontend-only. | `smoldering-cinder-gateway.md` §Chunk 2 |
| Endgame Chunk 3 — WD 3D shader + structure panel | **done** | Shipped: smooth degenerate-sphere shader (granulation off via a log g gate so the TPAGB giant still boils; corona off; cooling color from Teff) + a data-driven layered structure cross-section (C/O core + DA atmosphere from the model; He buffer & thicknesses schematic, envelope thinning with log g; crystallization in the CORE with a log g-gated onset). Frontend-only. | `smoldering-cinder-gateway.md` §Chunk 3 |
| Endgame Chunk 4 — WR mode shell + HR + composition | ✅ done | Shipped: reversible `→ Continue: Wolf–Rayet` gateway (feh-threshold-gated), index-linear scrub over the φ9 sub-track (WN→WC landmark), HR axes to ~316 kK / logL 7, the **normal comp views** carrying the stripped-surface WN→WC→WO story from real data, WN/WC/WO subtype from surface composition, mass-stays-live re-snap, end caption narrates the un-modeled core-collapse. 3D = smooth hot-sphere placeholder (Chunk 5 owns the wind shader). 31/31 Playwright. | `smoldering-cinder-gateway.md` §Chunk 4 |
| Endgame Chunk 5 — WR 3D wind shader | ✅ done | Shipped: optically-thick-wind look (`star.js` `WIND_FRAG` + `wind` mesh) — limb-brightened electron-scattering haze + outward-advected value-noise filaments over the opaque hot sphere; reach/density read from `Z_surf` (smooth WN → clumpy WC/WO), honest Teff color (no chemistry hue — it'd contradict the spectrum placeholder), clamped-L intensity (not a measured Ṁ); **fit-to-frame extent** because the WR scrub opens on a huge R≈33 R☉ star that would clip. Frontend-only. Closes the on-hand-data endgame work. | `smoldering-cinder-gateway.md` §Chunk 5 |
| v sin i line broadening | ✅ done | Shipped: a pure client-side convolution of the baked spectrum with Gray's rotation profile (ε=0.6, per-pixel variable width Δλ_L=λ·v sin i/c, normalized → equivalent-width conserved), in `spectrum.js` only. Driven by the marker's real `v_rot_kms` (the MIST vvcrit axis) taken **edge-on** (sin i=1, the maximum projection — advisor settled: a lone inclination slider would be incoherent with no oblateness/gravity-darkening model). Scoped to the **main absorption cube only** (WD/WR/SN exempt — a slow degenerate remnant / wind-broadened WR / placeholder-ejecta). No refetch (re-broadens cached flux + redraws on v_rot change, memoized so resize doesn't reconvolve); caption note gated ≥120 km/s (the ~1-pixel floor). **Measured visible**: reachable rotators run 200–420 km/s (2.5 M☉ vvcrit0.4 → 212 km/s), Ca K core depth 0.43→0.30, Balmer lines shallow+widen; the Sun (v_rot=0) is untouched. Backend byte-unchanged. | `whirling-cohort-atlas.md` (Tier B) |
| Nine user-reported UX refinements (HR clipping · endgame captions/buttons · readout panel · panel jitter · gateway auto-appear · age-slider remap) | **A–E done ✅** | Triaged + chunked into 5 sessions: **A ✅** HR framing (items 5/6) — endgame views auto-fit to `fitBounds`; living view a **hybrid** (fixed teaching frame, expand only raw-overflow edges — preserves the inter-star scale cue, per advisor) not the planned widen-to-7; + WD living preview dashed→solid-grey **directional leader** (AGB tip→hot knee, `→ white dwarf`, SN-suppressed) fixing the "clipped/unclear" diagonal. **B ✅** endgame quick-wins — 7 WR SED caption now branches on `endgameWR` (wind-not-dynamo, core-collapse not WD; the surviving "white dwarf" is a corrective negation) · 8 Lane–Emden-for-WR = *generic intro adequate, no fix* (WR shows generic, WD shows polytrope hint) · 9 `.endgame-back` restyled to compact filled-accent yellow. **C ✅** layout (items 1/4) — 1 readout split into its own draggable `readout` panel (saved layouts append it last; Reset restores authored order); 4 jitter cured by a **user redirect**: instead of reserving *empty* space, the WD/WR gateway button is **always placed but `disabled`/greyed until end-of-life**, where it enables and "lights up" (same box → no resize) — the SN dead-end note likewise foreshadows from ZAMS (user-confirmed). **D ✅** gateway auto-appear (item 3) — repro **disproved the "never appears" logic bug**: post-C the button *does* auto-appear (~0.6 s, no age nudge). Root cause is **latency/perception** — `refreshTrack` clears the gateway children then the ~1 MB `/endgame` fetch (serialized after `/track`, + a redundant double-fetch) leaves the reserved slot **blank** for ½–2 s, reading as "nothing happened." Fix (2 parts, 2nd advisor consult): a muted *"Computing the star's fate…"* placeholder (gated on an in-flight `endgameLoading` flag, **not** `!!eg` — the `!!eg` first cut was dead code that stuck the spinner on a fetch failure) **+ a safe de-dup** (`refreshTrack` tail `updateLiveGateway()`→`updateGateway()`) that stops the redundant 2nd `/endgame` from discarding the already-landed preview response → button **+474 ms vs +628**; touches no token internals. + the **deferred resnap note now moved below the sliders** (grows the panel downward, not shoving the controls). Verified across WD↔SN↔WR both ways + a `/endgame`-abort (no stuck spinner). Then the true root was **also done (user opted in)** — a type-only **`/endgame?meta=1`** (~120 B vs ~1 MB; adds `has_states`) + a purely-additive frontend `fetchEndgameMeta()`/separate cache that **structurally decouples** the button from the 1 MB (it lights from the tiny meta while the full fetch still pre-warms preview/enter — the load-bearing slam-to-end prefetch stays eager, so bandwidth is unchanged). Warm-localhost saving is **modest (~15–30 ms on the /endgame hop**; the "66%" earlier claim was a cross-session artifact), bigger when the full is cold/contended; cold first call unchanged (placeholder covers it). + advisor-caught **enable-gating** (`disabled=!atEnd||!eg`) so the button can't dead-click in the meta→full gap (the exit→re-enter path). 1 meta+1 full per change (no double-fetch), all abort combos no-stuck-spinner, 138 pytest. **E ✅** age-slider remap (item 2) — the slider is now **linear in EEP, not age**: the marker is **picked straight from the already-fetched track by row** (no `/state` fetch — like the WD/WR scrubs), so the dead MS plateau (85–90% of the old linear-age slider) is gone and every phase gets fair, **mass-invariant** travel (post-MS = a constant 58.5%; the MIST track is a fixed 606 rows split MS 42/RGB 25/CHeB 17/EAGB 17% at every mass). Advisor reframed it as two decisions, both settled by measurement: (1) the marker MUST come from the EEP coordinate, not an age round-trip — flat-age bands (the 1 M☉ CHeB **blue loop** = 48 EEP rows over ~4,800 yr) would be degenerate under `/state?age=`, killing age-fetch/log-age/piecewise-in-age; (2) **linear-in-EEP** (matches `comp.js` `xOf(eep)` exactly → age thumb & comp marker in lockstep), not piecewise. Pick-from-track over a backend eep-fetch: no Protocol change, fetch-free instant scrubs, and it *simplifies* — landmarks position by row index, `ageValue` is a derived honest readout, the razor-sharp off-by-one is obsoleted. Preserve-absolute-age on a mass change (thumb jumps; spring-back kept). Both advisor gates verified in the running app: dead bands animate (CHeB Teff×~1400 K/L×~50, RGB Teff 9–12 kK; EEP strictly advances), Sun on the §10 anchor under nearest-row (L=1.07/Teff=5835/4.63 Gyr — within 0.3%/0.6 K of exact, no scalar-interp needed); gateway greyed through MS/EAGB, enables only at the true end; zero page errors. Frontend-only. | `polished-cinder-frame.md` |

| **Asteroseismology panel** (outward quartet — Axis C) | **planned** | A genuinely new observable: the star's p-mode oscillation spectrum. ν_max ∝ g/√Teff and Δν ∝ √ρ̄ (the seismic scaling relations, solar anchors 3090/135 µHz) + a schematic power spectrum and the beautiful **échelle diagram** — how Kepler/TESS weigh & age stars. Frontend-only (`seismo.js`, derives from the served M/R/Teff, no backend touch — the `sed.js` precedent). Tier-2 (scalings labeled approximate); **gated to the convective-envelope regime by the physics** (hot-star pulsation → the instability-strip overlay). Gate 0: Sun reproduces 3090/135, a red giant rings at ~34 µHz. | `outward-quartet-atlas.md` §Axis C |
| **Habitable-zone band** (outward quartet — Axis D) | ✅ **done (D1, 2026-07-10; frontend-only, Playwright 1440+390 zero console errors)** | Shipped: the circumstellar HZ from L + Teff (**Kopparapu 2014**, ApJ 787 L29, corrected coefficients, 1 M⊕) drawn on the true-size scale bar — a solid **conservative band** (runaway greenhouse → maximum greenhouse) with dashed **optimistic** edges (recent Venus → early Mars). Pure helper `hz.js` (unit-checkable) + `scale.js` `setHZ`; `#hz-toggle` in the scale panel, living-only (dropped on every endgame/stripped switch via the shared chokepoint + CSS-hidden there). **Coefficients validated pre-draw against the solar anchor** (advisor's measure-first reorder — a wrong coefficient is a plausible-but-wrong band Playwright can't catch): Sun → runaway 0.95 / maxGH 1.68 AU exactly. **The quartic DIVERGES outside 2600–7200 K** → `inRange()` gates the compute AND the band entirely (not just the caption; else NaN/absurd-distance breaks the draw); the band honestly blinks off for hot stars with an out-of-range caption. **Axis auto-widens** past R_MAX for a luminous giant (SN idiom) + swaps in outer-planet landmarks (Neptune anchors the tens-of-AU reach). Gate 0 PASSED through the runtime: Sun 0.98–1.7 AU straddling Earth's ring; the same 1 M☉ marched to 34–65 AU past Neptune at old age (Earth "left inside the conservative inner edge"); K/M dwarf zones correctly closer in. **Advisor caught + fixed a reversed directional clause** (faint-star "zone lies closer in", not "farther out" — hits most K/M dwarfs). Spectrum enters through Teff (the near-IR-albedo effect IS the Teff-dependence); UV/X-ray/flares deliberately OUT of the band, owned by the caption caveat. **D2 (HZ history / ghost trails of past positions) not built** — the live march-outward on the age scrub already carries the payoff. | `outward-quartet-atlas.md` §Axis D |

### 2. Backend / data touch — real, bounded effort

| Item | Status | Hook | Where |
|---|---|---|---|
| SED Chunk 2 — hot-star **wind thermal free–free excess** from Ṁ | ✅ done | Shipped: the one *predictive* non-thermal piece. `mdot_msun_yr` now on the spine (`Mdot` blended LINEARLY like `Vrot`, no `CACHE_VERSION` bump) + a solid teal-green λ⁻²·⁶ line in `sed.js` (absolute Wright–Barlow, drawn where it crests the photospheric floor). **Measure-first correction:** it's a mid/far-IR → sub-mm excess for **evolved hot supergiants** (peaks ~35–260 µm), NOT the ZAMS-O "mm/radio" the plan's table claimed — that table was ~4 dex too bright + circular (fixed via a ζ Pup anchor + a proven BB norm). 198 pytest. | `magnetic-ember-broadcast.md` §Chunk 2 |
| Rotation `vvcrit` axis (0.0 ↔ 0.4) | ✅ **done (all 4 chunks)** | The headline subpopulation control — MS surface-N/He enrichment, lifetime shift, lowered WR threshold, CHE. Real MIST v2.5 data, a **third provider axis**; 2-point ⇒ toggle/snap. **Gate SETTLED:** bit-identical ≤1.20 M☉, diverges ≥1.25 (the ~1.2 Kraft break). **Chunk 1:** provider keys by `(feh, vvcrit)`, snaps between buckets, `vvcrit=0.0` default byte-unchanged, `CACHE_VERSION`→10. **Chunk 2:** data-derived `rotation_status` honesty gate (has_grid/threshold/active). **Chunk 3 (3a/3b/3c):** API `vvcrit` param on `/state`/`/track`/`/endgame`/`/mass_range`/`/age_range` + `/rotation_status` route + real `v_rot_kms` (`surf_avg_v_rot`, `CACHE_VERSION`→11) + a **unified frontend "Rotation" control** (two regime-gated facets: the vvcrit track toggle + the relocated SED period slider); payoff proven via `track()` (20 M☉ N+He; m100 40 M☉ CHE). **Chunk 4 ✅** = fetched m075/m050/p050 vvcrit0.4 (171/169/170 tracks), so the rotating axis now spans the **full [Fe/H] axis** −1.0→+0.5 (coextensive with non-rotating); `has_grid` extends across it (was capped at the partial axis); MS surface-N enrichment confirmed real at the new grids (5.5×/4.6×/1.8× at 20 M☉, feh −0.75/−0.5/+0.5). 192 pytest (→194 with the follow-up within-bucket [Fe/H] interpolation tests, row below). **Rotation arc complete.** | `whirling-cohort-atlas.md` (Tier A) |
| [α/Fe] spectral axis | ✅ **done (Chunk 1 backend + Chunk 2 frontend + full [Fe/H] widen)** | Thick-disk/halo subpopulation, spectrum-only (track won't follow α — that's Tier D). **Path: Coelho 2014 via SVO ASCII** (`coelho_highres`, matched [α/Fe]=0/+0.4) — the host-baked Koester/TMAP WD-cube precedent, no Docker/pymsg/73 GB. Gates 0 & 1 CLOSED (α visible & Teff-gated; Na-D-opposite + hot-null controls). **Chunk 1 (backend, 254 pytest):** `fetch_coelho.py` + `bake_alpha_spectra.py` (4-axis Teff×logg×[Fe/H]×[α/Fe] cube, ragged-logg clamp-fill, no BAKE_VERSION bump) + `spectra.py` `alpha_spectrum_data()` + `/alpha_spectrum` + `test_alpha_spectra.py` (15, Gate 1 as regression). **Chunk 2:** frontend α toggle (`#alpha-toggle`, `spectrum.js`-owned) overlaying Coelho α=0 vs α=+0.4, three honest edges (hot ≥9 kK / cool-dwarf <3800 K / [Fe/H]-window, all runtime-derived), living-only (hidden in WD/WR/SN), spectrum-only caption. **[Fe/H] widen (2026-07-02):** cube now spans the FULL matched axis {−1.0,−0.5,0.0,+0.2} — cube (30×13×**4**×2×2400), 26.5 MB. A pure data re-bake (`fetch_coelho --feh -1.0,-0.5,0.0,0.2` + re-bake) + the one scope touch (fetch `--feh` default flipped to the full axis, ~17 GB). Verified through the route at the new nodes (−1.0/+0.2 return unclamped → the frontend fallback stops firing; α deepens Ca lines, −1.0 CaI 4227 +0.132); Teff intersection held at 30 (no shrinkage). **Widen beyond +0.2 (to +0.5) is blocked — α=0 exists in Coelho only at {−1.0,−0.5,0.0,+0.2}**, so +0.5 stars keep the honest fallback. | `whirling-cohort-atlas.md` (Tier B) |
| Rotating-axis "lies-between-metallicities" test | ✅ **done** | Parity gap (advisor-flagged at Chunk 4) now closed: two `test_rotation_axis.py` tests mirror the non-rotating pair — `..._lies_between_metallicities_rotating` (structural lies-between at vvcrit=0.4 + a contamination-style log-Teff discrimination clause) and `..._tracks_held_out_grid_rotating` (the one with teeth: hold out rotating p000, interpolate from rotating m050/p050, measured L median 0.7% vs ~2.6% for the wrong-axis blend). Both at mass 3.0 (above the Kraft break) so rotation actually bites. New `requires_mist_rotation_multifeh`/`_heldout_feh` markers. 194 pytest. | `whirling-cohort-atlas.md` (Tier A) |
| Spectrum **zoom / detail sub-band view** (the finer-bake prerequisite) | ✅ **done (2026-07-02, frontend-only)** | The honest payoff path from the density-skip below: preset band buttons (**Full · Ca H&K · Mg b · Na D · Hα · He 4686**) that reframe the x-axis onto a ~120–150 Å window — a pure client-side reframe of the SAME served data (no refetch), where the grid's already-served 2.5 Å sampling stops being sub-pixel and the line cores resolve. `spectrum.js` `viewBand` state + `viewWindow()` clamp; window-local vertical normalization (band continuum→1); adaptive nm ticks + "flux (band max=1)" label gated behind `viewBand` (full view byte-identical); **native sample points marked as dots** (the load-bearing honesty — a smooth polyline hides the sampling, so you can now judge whether a finer bake would help). Applies to the main cube **and** the [α/Fe] overlay (both curves get dots — the α Mg b/Ca deepening made legible at native res); reset-to-Full + row hidden in WD/WR/SN. Added `{lam:5175,"Mg b",maxTeff:8000}` to the main `LINES` (was α-only). **Measured/Playwright-verified 1440 + 390 px, zero console errors** (row wraps cleanly on phone; endgame hide/reset checked on-screen): Ca H&K splits cleanly into K 3933 + H 3968; Na D shows a **marginal double-dip right at the 2.5 Å limit** (6 Å ≈ 2.4 bins → caption stays generic, never claims a clean split — this band best illustrates *why* a finer bake could pay off); He 4686 lights up on a 34 kK O star; M-dwarf blue band legible. Backend byte-unchanged. | `graceful-toasting-thimble.md` §Next |
| Spectra density bump / hot-end / B-star detail | **skip — measured real-but-invisible (2026-07-02); zoom view now built (row above)** | Investigated + gated: a higher-res re-bake produces **no visible change on the FULL panel**. Two independent supports — (1) the recipe's own note that CAP18 `coarse`→`high`/`ultra` differ *only* in spectral R, which the bake resamples to the 2.5 Å λ grid; (2) display physics: the full panel shows ~6000 Å over ~1200 px ≈ **5 Å/px**, so the current **2.5 Å bins are already ≈1 bin/px** — higher source R cannot render at full width. It fails the measure-first gate (the VO-7400 / boron-b8 / 0.6 M☉ skip). BSTAR2006 already dismissed in §5a (LTE→NLTE only, adds nothing >30000 K). **The zoom view (row above) is now built**, so a finer bake is *reconsiderable* — but only where the dots in a zoomed band (e.g. Na D at the 2.4-bin limit) visibly under-sample the line; that is a measured, per-band judgement, still gated. | `graceful-toasting-thimble.md` §Next |
| Endgame Chunk 6a — WD spectra (**Koester DA**) | ✅ done | Separate rectangular (82 Teff × 13 log g) Koester DA cube, pure-H (no [Fe/H]), host-baked (no Docker/pymsg). New `/wd_spectrum` sibling + `fetch_koester.py` + `bake_wd_spectra.py`; DC-below-floor + 80 kK no-model honesty edges; phase-aware switch at log g 6.0; 15 tests. | `smoldering-cinder-gateway.md` §Chunk 6; recipe §8 |
| Endgame Chunk 6b — hot WD/CSPN spectra (**TMAP**) | ✅ done | Fills the >80000 K no-model gap (the ~100–190 kK central star). Host-baked `fetch_tmap.py` (SVO `tmap` H-rich slab, 72 models) → `bake_wd_spectra.py --tmap-dir` splices the >80 kK nodes onto the SAME WD cube (93×13×2400, `koester2-DA+TMAP-CSPN`); `regime="CSPN"` (logg-aware: the 55–80 kK contracting rise reads CSPN, a hot high-g remnant stays DA), `refreshWD` routes log g≥6 **or Teff>55 kK** (the main cube's OSTAR ceiling — advisor-caught: at >80 kK a rise row flashed "no model"). **Measured: NO ×π×10⁸ (SVO ascii is physical; seam ratio 1.005–1.021 → no rescale), log-linear blue-gap fill, log g-clamp honest (optical Δ 0.03 vs 0.41 DA), no BAKE_VERSION bump.** Residual no-model re-pointed at 190 kK. 157 tests (+4). | `smoldering-cinder-gateway.md` §Chunk 6 (6b); recipe §8b |
| Endgame Chunk 7 — WR spectra (PoWR) | ✅ done (**narrow-GO**) | A THIRD `/wr_spectrum` sibling over the PoWR wind-emission grids, keyed on (T\*, Rt) not (Teff, log g). Host-baked `fetch_powr.py` (per-grid tarballs, gal/lmc/smc) → `bake_wr_spectra.py` (**flat-node** cube — ragged (T\*, Rt) parallelogram, no rectangular void-fill) → `wr_spectrum_data` (T\*≈Teff, Rt from a Nugis–Lamers Ṁ, subtype/metal snap) → `spectrum.js updateWR` (emission draw, continuum-normalized). **The measured 7a gate found it a narrow-GO:** PoWR honestly covers only the cool, H-rich **WNh entry** (~10%); the stripped core is the hot/compact *evolutionary* surface (Teff 150–262 kK ≈ T\*, the evolutionary-vs-spectroscopic Teff gap), far off any *observed*-WR grid → honest no-model frame. 170 tests (+13). **Endgame arc complete.** | `smoldering-cinder-gateway.md` §Chunk 7; recipe §7a/§9 |
| **Core-collapse SN + NS/BH endgame** (fills the dead SN branch) | **COMPLETE — Chunk 0 ✅ · 1 ✅ (backend) · 2 ✅ (frontend) · 3 ✅ (3D fireball→remnant) · 4 ✅ (onion shell) · 5 ✅ (remnant branch NS/BH/failed-SN + cliff softening)** | Turn the core-collapse `type="SN", states=[]` dead-end into a real arc — a **⁵⁶Ni-powered semi-analytic light curve** (Arnett ⁵⁶Ni→Co→Fe tail + Popov/KW IIP plateau) + **homologous ejecta expansion** + NS/BH/**failed-SN** remnant + *maybe* an onion-shell/yield. **Hybrid sibling** architecture: classification stays in `PROVIDER.endgame()` (+ progenitor scalars, a `CACHE_VERSION` bump), the **computation** is a new `supernova.py` + `/supernova` sibling emitting BOTH photosphere `StellarState`s and a light-curve object. **3-tier honesty:** Tier-1 ⁵⁶Co tail slope 0.0098 mag/d (bulletproof anchor) · Tier-2 plateau shape from MIST M_ej/R₀ · Tier-3 peak ∝ M_Ni (**free slider** — not predicted). **Chunk 1 BUILT (backend vertical, 215 pytest):** `EndgameResult` scalars `pre_sn_radius_rsun`/`he_core_msun`/`co_core_msun`/`h_retained` (SN-branch only); `CACHE_VERSION` 11→12 (he/c/o core columns, Mcur-pattern); pure `supernova.py` sibling + `/supernova` route (bypasses PROVIDER for compute, calls `endgame()` for the progenitor; non-SN → honest empty). **R₀ = max over final-phase rows excl. the terminal artifact** (settled the open knob); light curve `w·L_p·rise + (1−w)·L_radio`. **Measured canonical 15 M☉:** plateau 1.83e42/138.7 d, Co-slope 0.00976. **Two advisor test corrections (measurement-validated):** Tier-3 scales the **tail** not the peak (IIP peak = the M_Ni-free plateau); Tier-1 slope measured on the **`L_radio` component** (0.00976 vs 0.01023 on `L_total` where the plateau bleeds in). No-plateau fallback (compact low-Z, R₀<300) unit-tested. **Chunk 2 BUILT (frontend gateway + light-curve panel):** a reversible `sn-mode` mirroring WD/WR — the dead SN note becomes a `→ Continue: Supernova` gateway button, the HR panel CSS-swaps to **Supernova light curve** and `hr.setSupernova()` redraws it as **L vs LINEAR days** (the straight ⁵⁶Co tail), the age slider becomes a linear-days time scrubber, the ⁵⁶Ni-mass slider (Tier-3, 0.001–0.3, debounced) lifts the **tail not the plateau**, and the **cited observed-photometry overlays** (`frontend/src/sn.js` — SN 1987A tail + SN 1999em IIP plateau, **published bolometric fits, not raw photometry**, cited in a visible caption) are the deferred **Tier-1 anchor**. Consumers get the photosphere `StellarState` via an explicit `{endgame:"sn"}` signal (not naive reuse — `logg≈−5` ejecta would trip the boiling-fireball gate). Playwright-verified 1440 + 390 px. **Chunk 3 BUILT (3D expanding fireball → remnant, frontend-only):** `star.js` `FIREBALL_FRAG` (additive `fireball` sphere, 3D value-noise fbm on a bounded time orbit, center-bright, color = the **honest blackbody Teff** — blue-white→orange→red IS the cooling, the one load-bearing cue; clamped-alpha single additive layer ⇒ no white-out) + `REMNANT_FRAG` (a small additive dot). `refreshSN` drives `snGrow` (early "expanding" beat) + `snFade` (late dissipation), threaded `{endgame:"sn", remnant, snGrow, snFade}`; as `snFade`→1 an **NS** emerges as a tiny hot blue-white dot (color labeled **evocative, NOT a Teff**), a **BH/failed** shows none — the frame "**winks out**." Measured: a BH-on-SN progenitor IS in-grid (**30 M☉ solar** → BH vs **15 M☉ solar** NS), both verified on-screen; 215 pytest still green (backend untouched). **Chunk 4 BUILT (pre-collapse onion shell, frontend-only):** `comp.js` `drawSNOnion` replaces the SN placeholder with the progenitor's onion cross-section, driven by the scalars `/supernova` already serves (no backend change). Real boundaries (remnant/CO/He/total) sized by **area ∝ enclosed mass**; inner Si/O–Ne **faint schematic** (MIST v2.5 ends before the iron core forms — the caption owns it); **NS/BH contrast honest** (BH swallows the C/O core → onion visibly lacks the copper band); ⁵⁶Ni an exaggerated slider-tied ring. Static across the time scrub, redraws on ⁵⁶Ni-slider/mass-re-snap (both verified live). Phone-width label-overflow bug caught + fixed. **Chunk 5 BUILT (remnant branch NS/BH/failed-SN + cliff softening; backend + frontend — arc COMPLETE):** the hard CO=7 NS/BH cut became a **labeled fallback continuum** `φ=smoothstep(CO_NS_MAX=7, CO_DIRECT=12, co_core)` softening the old cliff in the light curve AND the onion at once — the remnant grows `M_NS+(M_final−M_NS)·φ`, the ejecta shrink smoothly to ~0, the **ejected** ⁵⁶Ni `=M_Ni·(1−φ)` dims (deepest ash falls back first); new served fields `fallback_fraction`/`failed_sn`/`m_ni_ejected_msun`. **Two advisor blockers fixed:** (1) the **NS↔BH label flips at the remnant crossing NS_MAX~2.5 M☉, not CO=7** (else a mass-gap "BH" reads as a bug) → **30 M☉ solar is now a heavy NS, not a BH**; demos BH-fallback 40 M☉/[Fe/H]−0.5, failed 50 M☉/[Fe/H]−1.0; (2) a **failed SN does NOT reuse the homologous photosphere** (v blows up, Ni→0 → log0) — stays at R₀ and dims (the "disappearing supergiant"), curve floored positive ~3 dex below a real IIP. **3D reconciled** (Chunk 3 made all BH wink out): NS→fireball+dot, **BH-fallback→fireball→dark invisible remnant (no dot, not a wink-out)**, **failed→dim→black (winks out)**. Onion drops the rem≤CO clamp (remnant eats C/O→He→H), ring uses ejected Ni, failed onion ≈ all void; `hr.js` violet failed-SN banner; readout "% fell back" + "direct collapse". 220 pytest, zero console errors. **NO explosion mechanism** (aftermath only). **Verify = real observed light curves.** **Type Ia is NOT this arc** (binarity §3, off the WD branch). | `radioactive-afterglow-requiem.md`; memory `star-sim-supernova-remnant-endgame.md` |
| Real interior structure (MESA `profile.data`) | ✅ **done (the four mass regimes: 0.25 M☉ fully-convective M dwarf + 1 M☉ solar radiative-core+conv-envelope + the 1.3 M☉ transitional double-convective (core AND envelope) bridge + the 2/6/15/25 M☉ convective-core+radiative-envelope slices) + the metallicity axis at 1 M☉ AND 0.8 M☉ ([Fe/H]=−1/0/+0.5 — a partial 2D grid)** | Shipped the honest successor to Lane–Emden: `structure.py` sibling + `/structure` route (bypasses PROVIDER) serving a **real** MESA radial profile — ρ(r)/T(r)/P(r), X/Y/Z, and the **true convective/radiative split** (`mixing_type==1` ∪ Schwarzschild) — snapped to the nearest saved `profile.data` snapshot in (mass,[Fe/H],age), plus the **two canonical polytrope overlays (n=1.5/3, NOT fitted** — the departure is the lesson, advisor-settled). A **new radial-profile dict** (not a `StellarState`), its own MESA-profile parser (never imports a provider — §3). MESA-only (MIST has no profiles; live solve ruled out §2/§9): profiles self-run once via Docker MESA (solar recipe + profile snapshots, `backend/docs/mesa_structure_recipe.md`), gitignored under `data/mesa_profiles/`. Frontend `structure.js` = a live consumer wired into `paintState`; jumps between the handful of snapshots, snapped-far note off-grid. **Measured:** 1 M☉ mid-MS ρ_c≈190 g/cc, T_c≈1.66e7 K, radiative core + convective envelope base r/R≈0.75; near-ZAMS transient convective core. **2/6 M☉ = the mirror** (convective core under a radiative envelope, `expected_n`→3/2; 6 M☉ mid-MS ρ_c≈16, T_c≈2.95e7, R≈3.89, core r/R 0→0.131) — **NO runtime code change** (index globs the tree, snaps on the header); added `requires_structure_massive` marker + 3 flip tests + fixed 1 stale off-grid-snap test; advisor semiconvection-OR caveat re-checked → doesn't bite. Snapshot-selection is the risk (massive core shrinks toward TAMS → kept a healthy mid-MS Xc≈0.4 anchor). **15 M☉ slice added** — the SN arc's canonical progenitor (honest structure where the SN feature lives; was snapping to 6 M☉), the **deepest** convective core (ρ_c≈5.9, T_c≈3.5e7, R≈6.67, core 0→0.178, mid-MS anchor profile11 Xc 0.41); NO runtime change, +1 gated test (`requires_structure_massive` already covers it, no conftest change), advisor OR-clause re-checked at 15 M☉ (extra cells all r/R≥0.97 → no over-shading). 13 tests (233 pytest), Playwright-verified 6 M☉ + 15 M☉. **0.25 M☉ M dwarf added — the third regime** (advisor chose it over the literal "Next: other-Z": a new *regime* is visible-by-construction, so the honesty gate is met automatically; other-Z is subtler — envelope-CZ depth — do it after): a **fully-convective** star, one convective zone spanning 0→1 (whole star), `expected_n`→3/2, ρ_c≈135–138/T_c≈7.4e6 (below the Sun's 1.5e7, weak pp)/R≈0.247. Two advisor-flagged run changes: **`max_age=2d9` replaces the TAMS stop** (a 0.25 M☉ MS lifetime is ~10¹²⁻¹³ yr, Xc barely moves) + **ship a settled-MS profile, not a pre-MS-contracting one** (profiles 20/21/22 of 22, L settled ~0.0105 L☉). **The polytrope-honesty INVERSION** (measured before writing it into the caption): the real ρ **hugs n=1.5** (~1–5%), far above n=3 — the one bucket where the idealization *works*, inverting "the departure is the lesson." NOT a pure drop-in: +`requires_structure_lowmass` marker + 2 tests + off-grid snap probe 0.3→0.15 (0.25 is the new floor) + a small `structure.js` caption refinement. **236 pytest**, Playwright-verified 1440 (ρ overlaps the n=1.5 dash, whole-panel convective shading). **The metallicity axis added — the first non-mass slice** (1 M☉ at [Fe/H]=−1.0 and +0.5): the solar-abundance-problem effect, lower Z → more transparent envelope → **shallower convective envelope** (base at higher r/R). **Gating measurement first, at matched Xc not age** (a metal-poor 1 M☉ is hotter/shorter-lived): mid-MS envelope base **0.70 / 0.75 / 0.95** at [Fe/H] **+0.5 / 0 / −1** — visibly, monotonically distinct → clears the gate. **NOT a core-type flip** (advisor-predicted): core stays radiative, `expected_n=3` at every Z; the whole payoff is the envelope-depth band. [Fe/H]=−2.0 run too but **not shipped** (base 0.99, fragments into tiny zones, indistinguishable from −1). **MESA gotcha: change `Zbase` too, not just `initial_z`** (round Z → clean [Fe/H] label auto). **Runtime NO code change** (the index already snapped mass→feh→age; frontend already passed the marker's [Fe/H]); accompanying = data + `requires_structure_multifeh` marker (gated |[Fe/H]|>0.3) + 1 trend test + a **[Fe/H]-snapped-far note** in `structure.js` (the Z grid is 1 M☉-only) + the conv.base tooltip's Z-link. **237 pytest**, Playwright-verified 1440 (band r/R 0.69→1.0 at +0.5, sliver at 0.97 at −1). **The [Fe/H] axis now lives at a SECOND mass — the 0.8 M☉ K dwarf** (recipe §11) → the first genuine **partial 2D (mass×[Fe/H]) grid**: same regime as the Sun but a deeper envelope, base **0.66/0.69/0.81** at [Fe/H] +0.5/0/−1 (matched-Xc, monotone, unfragmented, core stays radiative n=3). **Chosen by measurement over two rejects:** 6 M☉ convective-*core* edge (the novel Z-on-a-core lesson) **failed** (~0.02 shift, non-monotone below Xc 0.5 — massive stars respond to Z in R/Teff not core fraction); 1.3 M☉ envelope fragments into ~0.99 slivers by [Fe/H]=−0.5 (real mixing_type, checked). **Runtime NO code change** (mass→feh→age snap; Z-less mass→solar fallback, both verified through the real path); +3 data dirs + 1 test (mass-parametrized helper, existing `requires_structure_multifeh`) + a `structure.js` comment refresh. **238 pytest**, Playwright-verified 1440 (conv. band shallows 0.659→0.694→0.807). **[Fe/H]-axis clean window now MEASURED to its edges → closed (recipe §12):** 0.6 M☉ non-ship (spread ~0.09<0.1, +0.5≈0.0 — compression; a deeper envelope gives a *smaller* Z-response, not bigger) + 1.1 M☉ non-ship (spread big but −1.0 fragments to 2 zones AND core type flips conv/rad/conv across Z); 0.9 M☉ clean-but-redundant. Usable window = **0.8–1.0 M☉, fully covered**; no data/test/code shipped (negative result, docs only). **The 1.3 M☉ transitional double-convective slice now BUILT (recipe §13) — the FOURTH regime:** a convective core AND a convective envelope at once, with a **radiative gap** between (the three-layer sandwich conv/rad/conv) — the bridge between the Sun and the massive stars, the one slice showing both convective zones simultaneously. Standard drop-in bucket (initial_mass=1.3, solar Z, **normal TAMS stop** — 3.1 Gyr MS, no `max_age` hack); ships **4 MS snapshots** (`solar_1p3Msun/`, anchor profile10 ρ_c≈156/T_c≈2.11e7/R≈1.66/core 0→0.059/**envelope base 0.885**); at TAMS the core vanishes → not shipped. **NO runtime code change** (drops in as a bucket). **Advisor's correctness trap avoided:** the caption's DEEP-envelope gate `z[0]<0.95` excludes the 6/15/25 M☉ razor sub-surface sliver (r/R≈0.994) so they aren't relabeled double-convective (verified via the real path + Playwright — 6 M☉ caption unchanged). +`requires_structure_transitional` marker (**narrow 1.1≤mass≤1.5** — must exclude both the Sun and the 2 M☉ slice, else fail-not-skip) + 1 test (three-layer probe: conv@0/**rad@0.5**/conv@0.95) + a `structure.js` caption branch. **239 pytest**, Playwright-verified 1440 (two distinct shaded bands, radiative gap unshaded between; zero console errors). **Next (a USER choice, not auto-pursued):** the interior-structure ladder is broad now — remaining threads are the rotation-atlas stragglers / spectra-density (see the rows above). | `backend/docs/mesa_structure_recipe.md`; memory `star-sim-interior-structure-mesa.md` |

| **Isochrone / cluster view** (outward quartet — Axis B) | ✅ **done (B1 backend + B2 frontend, 2026-07-10; 412 pytest, Playwright 1440+390 zero console errors)** | Shipped: the transpose of a track — all masses at ONE age, a coeval-cluster locus on the HR panel, the **main-sequence turnoff ringed** (the age clock), the user's star sitting on it. A sibling `isochrone.py` + `/isochrone` (+ `/isochrone_status` gate), bypasses PROVIDER. **Data source = MIST's *published* v2.5 `.iso` grid** (version-consistent with the live tracks, so the star lands EXACTLY on the MS locus — Sun-anchor test passes; no interpolation-of-interpolation, so the from-tracks EEP cross-check the plan proposed was **dropped as advisor-flagged** — it reintroduces the very hazard the `.iso`-primary choice avoids). **The size surprise (measure-first pivot):** each `.iso` is ~550 MB of 170-col text (the whole v2.5 iso axis ships as ONE 6.7 GB tarball — no per-[Fe/H] download), so `fetch_mist_iso.py` streams it once and **bakes** each solar-scaled ([a/Fe]=0) node in the track domain to a ~2.6 MB per-(feh,vvcrit) `.npz` (the tracks' `_parsed_tracks.npz` precedent; `BAKE_VERSION`); 7 nodes −1.0…+0.5 (denser than the tracks' 5 — the iso grid has ±0.25 steps). **Turnoff = bluest MS-phase row, NOT global max Teff** (advisor catch, dramatically confirmed: old/metal-poor isochrones carry hot post-AGB/WD stars at ~10⁵ K vs an MSTO ~6000 K — a naive max would be off 20–50×). B2: `#isochrone-toggle` (gated on `/isochrone_status`), `hr.js setIsochroneOverlay`/`drawIsochrone` (Teff locus + ring + on-locus marker), owns the HR panel like the He/α overlays (mutually exclusive with them), the cluster **ages with the age slider** (node-bucketed refetch), living-only. Gate 0 measured through the real path (turnoff monotone cooler+fainter+lighter 0.1→12 Gyr). **B3 (decoupled cluster-age slider) BUILT 2026-07-10** (frontend-only, no backend change — the route already served `available_log_ages`): `#iso-decouple-toggle` reveals a grid-exact log-age slider so the cluster ages on its OWN clock (the "cluster aging" movie — turnoff marches 27.2 kK/17.7 M☉ @10 Myr → 5.6 kK/0.92 M☉ @12.6 Gyr); only age decouples ([Fe/H] stays the star's), coupled-by-default, the star marker slides OFF the locus (the age comparison). **Hosting also DONE** (`mist-iso-baked-v1`, the 10th tag — the ~18 MB baked npz set past the 6.7 GB fetch). Axis-B arc is now fully complete. | `outward-quartet-atlas.md` §Axis B |
| **The observer's view** — distance + extinction + observational CMD (outward quartet — Axis A) | **A1 ✅ BUILT (backend + data, 2026-07-10; +10 tests)** · A2/A3 frontend planned | The theory→telescope bridge, the biggest conceptual gap the sim has: a distance + A_V/R_V axis that dims & reddens the served spectrum → synthetic photometry through **real SVO filter curves** → an **observational color–magnitude diagram** (the observer's HR). **A1 as built:** the Gate-0 prerequisite was MEASURED first — the main absorption cube is **absolute physical surface F_λ** (the Sun integrates to **98.2%** of the Planck in-band σT⁴; 9000 K → 102%), confirming ×(R/d)² gives real absolute mags. **The advisor's load-bearing catch: the cube stops at 8999 Å**, so Gaia G/RP + 2MASS JHK are uncomputable/truncated → **the anchor swapped from M_G to M_V, flagship CMD = (B−V, M_V)** (the classic observational HR diagram, still composes with Axis B into a cluster CMD). Clean in-cube bands = **Johnson–Cousins (Bessell) B, V + Gaia BP** (verification). `fetch_filters.py` → committed `star_sim/data/filters.json` (SVO transmission + Vega ZeroPoint(Jy) + **DetectorType** per band — 0 energy B/V, 1 photon BP). `photometry.py` (pure sibling, imports only numpy/stdlib — not even StellarState; AST-tested no-provider): CCM89 reddening A(λ)/A(V) + a **vectorized** synth core over a flux stack (for A3's track+isochrone, no N round-trips), DetectorType-aware band weighting, Vega-ZP-ratio mags. `/photometry` composes `spectrum_data` (full-res) + synth like `binary_pair`, returns absolute M_X + apparent m_X + (B−V)₀/E(B−V)/distance modulus. **Gate 0 PASSED through the runtime:** Sun M_V=**4.832** (t 4.81±0.05), distance modulus **exactly 10.000** over 10 pc→1 kpc, E(B−V)=0.296 at A_V=1. **Advisor-settled: use SVO ZeroPoints, NOT a Vega spectrum** (empirically ruled out — M_V nails it; a Vega SED is a rabbit hole with no astropy) — the synthetic solar B−V=**0.612** is ~0.04 blue of 0.65, a known **B-band ZP-convention** offset (SVO Bessell.B 3908.5 Jy vs lit ~4000–4060) that is **common-mode** across star/track/isochrone so it CANCELS in relative CMD placement (color tolerance relaxed to ±0.05). **Next = A2** (frontend Observer control group: distance/A_V/R_V → reddened overlay + mag readout) then **A3** (the observational CMD panel — the capstone, composes with Axis B). | `outward-quartet-atlas.md` §Axis A |

### 3. Big engines — formerly "out of scope," now candidates *with a cost* ("nothing is out of scope")

| Item | Status | Hook | Where |
|---|---|---|---|
| Binarity / mass transfer — **stripped star (a) + companion (b)** | **Path (a) Chunks 1–3 ✅ COMPLETE · Path (b) Chunks 1–4d ✅ ALL BUILT (companion on the HR + drawn in 3D + Roche geometry, the POSYDON two-body time series with a real co-evolving movie, free M1/q/P "Custom orbit" sliders, and now a [Fe/H] metallicity-bucket picker spanning the full 8-bucket POSYDON axis) · the CE/compact-object tail's **Chunks 1a (backend) & 1b (frontend render) & 1c (full 8-bucket [Fe/H] axis + free M_star/M_co/P custom sliders) are ✅ BUILT** (2026-07-08, `posydon_co.py` + `/co_binary_track`, 333 pytest [+23 total] — schema recon found `history2` absent grid-wide so this is a genuinely new sibling, not a `posydon.py` branch; real CO-mass growth via accretion + a schematic accretion-luminosity cue bounded ≤3.46x Eddington grid-wide (gated off POSYDON `unstable_MT` CE/merger tracks — see Chunk 1c below). **Chunk 1b** = a standalone curated demo inside stripped-mode (its own `.co-binary-view`, mutually exclusive with the HMS-HMS movie): HR shows ONLY the living star (a point-mass CO gets no luminosity point), 3D + Roche draw a schematic BH ring / point-mass marker (persistent, NOT the SN "winks out"), per-step Roche via the reused `binary.track_roche_geometry` (SimpleNamespace adapter), the η·Ṁ·c² accretion cue surfaced in the age-slider caption relative to the star's own L. Caught+fixed a slow-in-flight-fetch mutual-exclusivity race + an advisor-flagged readout/scale/MK false-data leak. **Chunk 1c** (data + frontend + one backend correctness fix, mirrors HMS-HMS 4c/4d, no runtime-axis change — `co_binary_track` was already snap-always): baked the 7 non-solar buckets → the full 8-bucket [Fe/H] axis; a `#co-binary-feh` picker + a "Custom system…" reveal of three **log** sliders, the CO custom axis being **(M_star, M_co, P) not (M1, q, P)**. **The full-grid pytest caught a real artifact:** the accretion-cue Eddington bound was solar-only characterized (2–3.5×); the metal-poor grids reach **505,221× Eddington** on a few rows, ALL on POSYDON `unstable_MT` (CE/merger) tracks (the served caption would've shown ~10^13× the star's own L, newly reachable via the picker/sliders) — gated the η·Ṁ·c² cue off `unstable_MT` → bounded ≤3.46× grid-wide, stable X-ray-binary payoff preserved. See `tempered-lineage-inspiral.md` + memory `star-sim-co-hms-rlo.md`) · `CO-HeMS`/`CO-HeMS_RLO` (double-compact-object channel — a separate extraction) and the population-overlay follow-on stay unscoped** | The biggest lever — **~70% of WR are binary-stripped**. **Path (b) Chunk 4d ✅ (frontend-only, no backend change, Playwright 1440+390 zero errors):** the [Fe/H] metallicity-bucket picker — `/binary_track`/`/binary_track_meta` already took a `feh` param and `binary_track_meta` already served `available_feh`, but `main.js` had it hardcoded to `feh=0`. A `<select id="binary-feh">` populated from the real bucket list applies to WHICHEVER system is showing (curated demo or "Custom orbit"), unlike M1/q/P which only ever drive "custom"; kept visible during an active co-evolving movie so metallicity can be compared without leaving it. Switching buckets refreshes `/binary_track_meta` first (each metallicity is its own grid, so M1/q/P bounds differ) before re-fetching the track. **Verified through the real app:** the select lists all 8 real buckets; the same (M1=286 M☉ snapped, q=0.6, P=3.73 d) system gives "stripped + companion" at solar but "stable mass transfer" at [Fe/H]=−4.0 — a real outcome change from the metallicity axis, not a cosmetic swap. **Path (b) Chunk 4c ✅ (frontend-only, no backend change, 310 pytest unchanged, Playwright 1440+390 zero errors):** a fourth "Custom orbit…" picker beside the three curated demos reveals free M1/q/P sliders (log-scale M1/P, linear q) driving the SAME `/binary_track` route — it was already fully general (snap-always over the whole grid), Chunk 4b's curated demos were only a UI de-risking choice. Grid bounds via `/binary_track_meta` (3.92–286.4 M☉, 0.1–5179.5 d, q 0.05–0.99); a note line always states the TRUE snapped node + outcome, never the raw drag. **One real bug the first Playwright pass caught:** the sliders panel appeared before the initial fetch resolved, so an early drag landed while `binaryView` was still false and got silently dropped — fixed by revealing the panel only once the track actually loads (the same point the demo/Back buttons already gate on). Verified end-to-end (enter → drag P to the merger node → drag M1 far off-grid with an honest snapped-far note → scrub → back → re-enter preserves values). **Path (b) Chunk 4b ✅ (frontend + a small backend addition, 310 pytest [+4], Playwright 1440+390 zero errors):** the two-star TIME render — a deeper reveal INSIDE stripped-mode (mode stays `"stripped"`), three curated demo systems (same donor M1=8.83/q=0.6, only P differs: merger / the Gate-0 stripped+companion / detached), the age slider repurposed as a free index-linear system-time scrubber. `hr.js` gained a new drawing mode (`setBinaryTrack`) — both stars' full tracks + independent past/future splits + markers fixed-labeled "donor"/"companion" by IDENTITY (the reversal is the crossing, never a relabel). The Roche panel (Chunk 3) goes LIVE (`roche.js drawLive`) off a **new backend engine** — `binary.py`'s Roche math factored into `_roche_geometry_from_params(q,m1,m2,a,n_samples)` + `track_roche_geometry(steps)`, imported by `posydon.py` (sibling-calls-sibling, the `structure.py`→`lane_emden` precedent), decimated to 40 angular samples/step (~2ms vs the CSV path's 160-sample ~14ms) to keep a 271-step track fetch under ~1s; no bake-version bump. Both stars get a REAL Teff/R every step (an upgrade over the static snapshot's schematic donor tint); whichever star `mt_state` flags as overflowing gets its lobe filled + the stream (caption-honest: the real photospheric radius doesn't itself cross the lobe outline during RLOF — a measured local-L1-excess gotcha). 3D is free (`star.update(s1,{companion:s2})`, the exact Chunk-2 call, now per-frame). **Path (b) Chunk 4a ✅ (backend vertical, solar-first, 306 pytest [+19]):** the on-ramp built. User landed all 8 metallicity tarballs (~84 GB); only solar extracted+baked so far. Recon correction: the raw grid HDF5 is already per-run (`grid/run<i>/{binary_history,history1,history2}`), NOT packed `PSyGrid` — so `scripts/bake_posydon.py` reads it directly with plain h5py (no host-side POSYDON-loader demux needed). Filters non-converged/misaligned runs, verifies the run↔`initial_values` index identity, decimates >300-row tracks, writes a flat CSR `.npz` (33,121 tracks/1.48M rows/142 MB from a 4.79 GB source). Runtime `posydon.py` is h5py/POSYDON-free, snaps (M1,q,P,[Fe/H]) in normalized log-M1/log-P/linear-q space to the nearest track (§6, never interpolates). **Gate 0 CLOSED** through the real runtime (demo system M1=8.83/q=0.6/P=3.73 d): the mass ordering crosses live (donor 8.83→1.07 M☉, companion 5.30→5.94 M☉), period widens 3.73→6.94 d, `mt_state` fires a real detached→RLOF1→detached sequence. Two honest gaps vs the design doc's guess: no eccentricity column (HMS-HMS circularizes) and C/N/O-only composition (not the 16-metal MIST breakdown). `/binary_track` + `/binary_track_meta` bypass PROVIDER, snap-always. (Chunk 4b, described above, built this on: the frontend system-time scrubber + live Roche panel.) **Path (b) Chunk 3 ✅ (backend + frontend, 287 pytest [+8], Playwright 1440+390 zero errors):** "the mass-transfer geometry / Roche lobes" — a genuinely new TWO-STAR render (the orbital-plane figure-of-eight at Case-B RLOF). Advisor-settled Option B (RLOF *moment*, 2D plane, separation from a new `P_init` CSV col via Kepler). Pure `roche_geometry()` (Kopal potential; L1/L2/L3 by bisecting dΦ/dx; lobe outlines by radial march to the critical equipotential — **L1-tangent bug** fixed by stopping at first crit-crossing OR Φ-turnover so the lobes kiss) folded into `/binary_pair`; new `roche.js` panel CSS-gated to `body.stripped-mode.companion-on`. The RLOF donor is a bloated cool giant of un-modelled Teff → **schematic warm tint, never the stripped Teff**; companion at real modelled radius, compact inside its lobe. **The blocker (advisor): the cross-panel mass-ordering REVERSAL** (donor HEAVIER here vs LIGHTER post-strip everywhere else — same class as the Chunk-1 q-bug) owned by the panel intro + caption. **Path (b) Chunk 4 — the on-ramp to a REAL binary grid: RECON DONE + DESIGNED (no build; data-gated).** Measured discriminator (advisor): **POSYDON, NOT BPASS** — POSYDON ships co-evolved binary TRACKS (both stars + orbit vs time, HDF5; Zenodo DR2 DOI 10.5281/zenodo.15194708, ~10 GB/Z, CC-BY), which is what path (b) wants; BPASS is population synthesis (a separate sibling). `fetch_posydon.py` (recipe + h5py validator, host-side handoff) BUILT. Design in `entwined-consort-inspiral.md`: a new time-series two-body sibling `posydon.py` + `/binary_track` emitting paired StellarStates, a system-time HR inspiral + a LIVE Roche panel; **runtime-POSYDON-free but extraction may use POSYDON's PSyGrid loader host-side** (the MESA-structure precedent — the raw grid is packed `PSyGrid`, not per-run). **The build decision (which slice + when) is the user's, gated on the data handoff.** **Path (b) Chunk 2 ✅ (frontend-only, 279 pytest UNCHANGED, Playwright 1440+390 zero errors):** "the companion drawn in 3D" — the accretor rendered as a REAL second sphere beside the stripped donor. **No backend touch** (the companion state is already served by `/binary_pair`). **Honesty split (advisor-settled):** the *sphere* is Tier 1/2 honest — a real modeled single-star state → the FULL surface shader (real Teff color, relative size, granulation, limb darkening), unlike the evocative corona/wind/fireball; what's NOT modeled is the geometry BETWEEN the stars (no separation/orbit) → the side-by-side placement is SCHEMATIC, caption-owned (the un-drawn-orbit precedent). `star.js`: a 2nd `companion` sphere (own `ShaderMaterial`, per-star uniforms differ) + a `companionGlare` (7–27 kK accretor would blaze; un-glaring = "less real"), no 2nd corona; `update(state, {companion})` lays donor LEFT / companion RIGHT via direct `mesh.position.x` (camera-facing quads stay parallel under x-shift — no billboard math), relative log-size preserved (companion BIGGER than the compact donor IS the reversal in 3D), a shared `applyCompanionScale` fit factor to the tighter frame axis (`FRAME_HALF_H` precedent, re-run each frame → live-resize refit), both glares TEMPERED ×0.6 (glare-merge trap), `companionMat.uTime` driven in `animate()` (else boil freezes — fireball precedent); all offsets+`.visible` UNCONDITIONAL → toggle-off byte-identical (donor re-centered, companion hidden). `main.js` threads `strippedData.companion.state` + adds the schematic sentence to the caption. **Measured (advisor's high-mass concern, answered by the data):** the donor stays COMPACT at every node (R 0.16→0.88 R☉, mass 2→18) while the MS companion is large (1.83→7.76 R☉) → "companion bigger" reads everywhere; low-mass = a clean color contrast (2 M☉: donor blue-white 20 kK, companion warm-yellow 7 kK). **Scope call on record:** NO 3D text labels this chunk (caption + HR labels + size/color carry identity; left=donor matches the HR blue-left). **Next (b) Chunk 3: mass-transfer geometry / Roche lobes (a genuinely new two-star render), then a real binary grid (POSYDON/BPASS).** **The CE/compact-object tail (CO-HMS_RLO/CO-HeMS — post-SN NS/BH binaries, X-ray-binary/GW-progenitor physics) is now DESIGNED, not built** (2026-07-08): needs zero new downloads (already inside the 84 GB on disk, unextracted), Phase 1 of `tempered-lineage-inspiral.md`. **Path (b) Chunk 1 ✅ (279 pytest, Playwright 1440+390 zero errors):** "the companion drawn" — the second star of the Algol system on the HR, the mass-ratio reversal made visible, with **NO new dataset** (the advisor's steer — POSYDON/BPASS NOT needed): reuse the Götberg stripped DONOR (a) + the single-star `PROVIDER` for the COMPANION. New `/binary_pair` composes `stripped_star` (donor) + `PROVIDER.state_at(0.8·M_init, feh, τ=donor MS lifetime)` (companion); `binary.py` stays a PURE sibling (composition in the route). Baseline `M2_init=0.8·M_init` (grid **q=0.8**, non-conservative — no accretion-efficiency guess; advisor catch #1) observed at the donor's MS lifetime (companion still mid-MS since it's less massive — advisor catch #2). Frontend: an opt-in **"Show companion"** toggle (the literal (a)→(b) transition; (a) byte-unchanged when off) → `hr.setCompanion` draws a 2nd marker (dashed ring + dotted link + labels), auto-fit enclosing it, reversal caption + readout rows. Measure-first gate PASSED across the grid (reversal real everywhere; companion sane MS; L-ordering flips — the low-mass companion outshines the sub-luminous donor). Path (a) = the hot He-star a close companion strips (Götberg 2018), the ~70% headline; NOT a new provider (a binary product can't pass the single-star §3 interface) — a **sibling** `binary.py` + `/binary` snapping the committed Götberg grid, exactly like `supernova.py`/`structure.py`. **Chunk 3 ✅ (273 pytest, Playwright 1440+390 zero errors):** a FOURTH `/stripped_spectrum` sibling over the Götberg CMFGEN cube — a **flat-node** host-baked cube (`scripts/bake_stripped_spectra.py`) keyed on the SAME (Z, M_init) node `/binary` snaps (state↔spectrum consistency, advisor's tightest constraint), solar-only, vac→air + sort-by-λ, `normalised_spectrum.txt` continuum-normalized (no continuum estimation). `spectra.py stripped_spectrum_data` + `spectrum.js updateStripped` = a **bidirectional** draw (absorption dips at the subdwarf end, emission peaks at the He-star end). **Measure-first gate CLOSED through the runtime:** the absorption→emission sequence is real & monotone (2 M☉ pure absorption Hα 0.50 → 18 M☉ He II 4686 **7.2×** emission), distinct from the false O-star Balmer spectrum the Chunk-2 placeholder protected against. Bonus: fixed a pre-existing `.spectrum-zoom[hidden]` CSS leak (zoom presets leaked into every endgame + stripped mode — dead in WR/SN/stripped, working-but-meant-hidden in WD). Recipe §10. **Chunk 1 (266 pytest):** committed Z=0.014 table (`star_sim/data/gotberg_z014.csv`, SED-verified ≤0.07 dex), `stripped_star(mass,feh)` snap (Z,Minit)→StellarState + routing scalars (m_strip current mass, snapped_far flags, eligible range), snap-always route. **Gotchas:** Teff/Reff not T★; Z_surf=grid Z + (X,Y) renorm to 1−Z; mdot=None (no lifetime — not in the table). **Advisor correctness catch:** visibility gate keys Teff+Y_surf only, never L (L flips sign across the grid — M_strip≪M_init). **Chunk 2 ✅ (frontend-only, 266 pytest UNCHANGED, Playwright 1440+390 zero errors):** the reversible `mode="stripped"`, entered by a **mass-gated toggle** (entry-point i — a mid-life fork, not an end-of-life gateway) for eligible progenitors (2–18.2 M☉), exited by the shared endgame-bar "Back". **The three false-data leaks blocked (advisor):** HR keeps the progenitor's living track faint + drops the marker **blue-left** (reuses `hr.setEndgame([s],"stripped")` auto-fit); **comp = a NEW single-state SURFACE view** (`drawStripped` — the measured He-rich H/He/Z bar; core by-construction so NOT drawn); **spectrum → placeholder** (H-atm cube would paint a false O-star Balmer spectrum); **structure → not called**. classify `strippedLabel` via `opts.mStrip`; age disabled; SED tail off + honest for free (`mdot=None`). Snap-always resnap-note (no revert). Path (b) two-star co-evolution stays deferred (needs POSYDON/BPASS/MESA-binary). | `stripped-consort-unveiling.md`; `entwined-consort-inspiral.md` (the POSYDON on-ramp design); memory `star-sim-binary-stripped.md` |
| TPAGB thermal pulses | ✅ **done (already shipped via the WD gateway + a dedicated showcase)** | The deferred messy phase (φ5) was NEVER actually missing: `endgame()` snaps to ONE real grid track (no cross-mass/[Fe/H] interp — the §6 hazard sidestepped, exactly the row's own predicted path) and the WD cooling scrub already renders the ~601 TPAGB rows — the He-shell-flash loops were just **compressed into 12% of the slider** (`WD_FP`) to protect the ~100 kK central-star spike. **Measured faithful** (1–3 M☉ ≈ 0.26–0.34 dex/loop, metallicity-tracking; 5 M☉ collapses to ~0.02 dex — real hot-bottom burning, not an artifact). **The showcase (frontend-only, built):** an opt-in "🔍 Thermal pulses" toggle inside wd-mode (`.pulse-toggle`) that gives the TPAGB slice the WHOLE HR panel + slider — `hr.setThermalPulses()` draws surface **log L vs LINEAR kyr-since-TPAGB-onset** (~6× vertical decompression, the classic sawtooth: slow quiescent rise, brief flash, deep dip), the age slider scrubs the TPAGB rows linearly, and a **data-derived visibility gate** (`tpMedianPulseAmplitude ≥ 0.15 dex` via local-extrema detection) hides the toggle for the ≥5 M☉ near-flat pulses (the honesty gate — only offer the view where there's something to see). Marker rides with the past/future split; Playwright-verified 1440 + 390 px (flex-wrap on `.endgame-bar-top`), zero console errors. | `smoldering-cinder-gateway.md`; memory `star-sim-mist-provider.md` |
| Initial-helium / multiple populations | **✅ BUILT 2026-07-09 (Phase 2 COMPLETE — Chunks 2a/2b/2c, 387 pytest, Playwright 1440+390 zero errors)** | GC He-enhanced second generations (the "second parameter"). Built as a **what-if overlay sibling** `helium.py` + `/helium` (bypasses PROVIDER; imports only `state.StellarState` + the MESA parser's free helpers — never the live MIST spine). **Claude ran the 6-run MESA batch in Docker himself** (the user said "docker is on, bake it yourself"): baseline (Y=0.2704) vs. enhanced (Y=0.40) at 1/2/6 M☉ solar Z, inlists differing ONLY in `initial_mass`+`initial_y` (`Zbase=0.0152` in both — the Y axis leaves Z fixed, so the metallicity "change Zbase" gotcha does NOT apply). Data gitignored/MESA-local (`requires_helium_data`, hosted-data-assets pattern excludes MESA); **no npz bake** (advisor — 4 MB read raw by the reused parser). **Gate 2 PASSED at every mass** (matched-phase): enhanced-Y hotter+brighter+shorter-lived (τ_MS 2.7×/2.1×/1.9× shorter). **2b:** pairs keyed by ZAMS surface He (not dir name), exactly-two-per-mass asserted; snap-always route; lifted `_state_from_track` to module level for reuse. **2c:** a **light HR overlay, not a mode-swap** — `#helium-toggle` (mass 0.7–7, \|[Fe/H]\|≤0.25) → new minimal `hr.setHeliumOverlay` draws two Teff-coloured MESA trails ZAMS-labeled, **MIST track hidden** (the honesty rule); τ_MS (invisible on an HR diagram — advisor catch) surfaced in the caption; tears down on band-exit / any endgame entry. | design `tempered-lineage-inspiral.md` Phase 2; memory `star-sim-helium-overlay.md`; recipe `backend/docs/mesa_helium_recipe.md` |
| α-enhanced **evolution** | **✅ BUILT 2026-07-09 (Phase 3 COMPLETE — Chunks 3a/3b/3c, 397 pytest, Playwright 1440+390 zero errors)** | α-enhanced MESA tracks so HR position + lifetimes follow α (vs the Tier-B spectrum-only version already shipped via Coelho). **The "heaviest build" collapsed to Phase-2 difficulty (advisor):** MESA ships only solar-scaled opacity tables (Type-1 on the MS; Type-2 only H-poor → no α on the MS), and Salaris/Chieffi/Straniero 1993 show an α-enhanced track ≈ a scaled-solar track at the **equivalent total Z** to a few percent (below what this sim resolves) — so it's a **Z-only change** (`[M/H]=[Fe/H]+log10(0.638·10^[α/Fe]+0.362)`; [α/Fe]=+0.4 → Z_equiv=0.029862, Y fixed 0.2704, Zbase moves here unlike the Y axis). Claude ran the 6-run MESA batch in Docker himself; Gate 3 PASSED (cooler/fainter/longer at every mass, opposite of He — measured via the parser that skips pre-MS). Sibling `alpha.py` keys the pair by ZAMS Z, `alpha_fe` DERIVED from the Z ratio (inverted Salaris). Frontend = a LIGHT HR overlay sharing the He slot (mutually exclusive), Coelho-paired caption (the track sees only total Z; α's signature is spectroscopic). **Completes the whole `tempered-lineage-inspiral.md` plan.** | `whirling-cohort-atlas.md` (Tier D); `tempered-lineage-inspiral.md` Phase 3; memory `star-sim-alpha-overlay.md`; recipe `backend/docs/mesa_alpha_recipe.md` |
| **Coeval-ensemble overlay (BPASS)** — a whole population beside the one star | **✅ ARC COMPLETE 2026-07-10 — Chunk 1 (SED overlay) · Chunk 2 (HRD number-density overlay) · Chunk 3 (hosting, both cubes); 405 pytest, Playwright 1440+390 zero errors** | The first **ENSEMBLE** sibling — a coeval population (10⁶ M☉ born together at the marker's [Fe/H], seen at its age) overlaid on the SED panel, single-star vs. +binaries; the payoff = the **magenta wedge** (binaries keep it UV/ionizing-bright: measured **~256× ionizing @40 Myr, ~26× FUV @1 Gyr**, optical unchanged). A **separate sibling** from POSYDON (co-evolved *tracks*). **Chunk 1 as built:** `scripts/bake_bpass_spectra.py` (host, λ log-rebin-average 100000→1200) → `data/bpass/bpass_ssp.npz` (4.1 MB gitignored); `bpass.py` (pure numpy/stdlib, NOT even StellarState) snaps ([Fe/H] linear, age nearest-in-log10), f_ν→f_λ; `/population` + `/population_status`; `sed.js setPopulation` draws both curves co-scaled to one shared reference (gap IS log(F_bin/F_sin)) + wedge fill, decluttered; `#population-toggle` opt-in, not mass-gated. **Gate 0 measured off the raw HDF5 AND visible through the runtime.** **The build gotcha (Playwright, not tests):** a wrong dict-key drew nothing → looked like a fatal 14-decade-axis problem → one-line fix; verify data reaches the canvas before blaming the representation. **Chunk 2 ✅ BUILT 2026-07-10 (HRD number-density overlay; 405 pytest [+2], Playwright 1440+390 zero errors):** the HR-panel twin of the SED wedge — at the marker's ([Fe/H], age) the coeval population's star count per (logTeff, logL) cell, single vs. +binaries, drawn as a translucent cloud on the LIVE HR panel with the user's star as one point in it. **Data is a DIFFERENT release** from Chunk 1's v2.3 spectra (that record is spectra-only) — the BPASS **v2.2.1 `hrs`** files, RANGE-extracted from the 1.5 GB starter-kit Zenodo zip (10.5281/zenodo.7340797) pulling only the 26 needed members via HTTP ranges + `zipfile`. Schema from `hoki.load._hrTL` (TL type, 3 surface-H blocks, z-code→[Fe/H] in the FILENAME). `scripts/bake_bpass_hrd.py` → `data/bpass/bpass_hrd.npz` (~1.6 MB sparse); `bpass.py population_hrd()`+`hrd_available()` over a 2nd cube (`BAKE_VERSION_HRD`); `/population_hrd` + `has_hrd` on `/population_status`. Rides the EXISTING `#population-toggle` (one concept, both panels); `hr.setPopulationHRD` draws MAGENTA strict-binary-only cells (blue stragglers / stripped-He — the literal Gate-0 cells single evolution leaves empty) + faint CYAN shared cells + a legend, `MIN_COUNT=0.01` cutoff. **Gate 0 (measured, solar 40 Myr):** single hot-region count 0 vs binary ~283; ~33× more stripped stars. **Build gotcha (TWO passes, Playwright + advisor, not the tests): (1)** a too-narrow 4-decade alpha range hid the whole magenta payoff; **(2)** the over-correction painted ~1e-10-star cells as a false magenta FLOOD (the false-caption class) → fixed with `s<1e-3·b` + the count cutoff so the picture IS the measured claim. HRD hosting rolled into Chunk 3 (`_ASSETS` grew a 2nd entry; v2.2.1 starter-kit CC-BY 4.0 confirmed before upload). **Chunk 3 ✅ (hosting):** `fetch_bpass_baked.py` (the `fetch_coelho_baked.py` single-cube precedent — flat `{filename: sha256}`) pulls the 4.1 MB `bpass_ssp.npz` from the `bpass-baked-v1` Release (CC-BY 4.0, cleanest grant); no loader/test change (the cube has no MIST-style raw-source fingerprint), only `bpass.py`'s `_MISSING_HINT` now leads with the baked path; verified end-to-end into a fresh `STAR_SIM_BPASS_DIR` (fresh download → runtime serves the ionizing wedge from downloaded bytes). Chunk 3 now hosts BOTH cubes (SED 4.1 MB `bpass_ssp.npz` v2.3 + HRD 1.6 MB `bpass_hrd.npz` v2.2.1). **The BPASS coeval-ensemble arc is COMPLETE on both panels** (SED wedge + HRD cloud, both hosted). | `coeval-ensemble-overlay.md`; memory `star-sim-coeval-ensemble-bpass.md` |
| Live solver / reduced nuclear network | idea | The ultimate "any star" capability; large, only worth it if the grid approach hits a real wall (spec §9). | `whirling-cohort-atlas.md` (Tier D); spec §9 |

### 4. Minor / probably-skip — recorded so we don't re-propose them

| Item | Status | Hook | Where |
|---|---|---|---|
| Gravity darkening / oblateness (von Zeipel) | ✅ **done (both chunks — arc complete)** | The honest capstone to the rotation axis — driven by REAL ω from v_rot (not cosmetic). **Gate 0 PASSED:** median MS rotator ~8% bulge/8% ΔTeff, fastest reachable (5 M☉ [Fe/H]+0.5) a genuine 37% bulge/4900 K Regulus — all from vvcrit=0.4 data, no exaggeration. **Chunk 1 (frontend-only):** `gravdark.js` (Roche ω, volume-preserving oblate spheroid, von Zeipel β=0.25 pole-hot/equator-cool) + `star.js` surface-shader gradient + oblate scale + a fixed 3/4 tilt (edge-on masks the gradient — poles at the limb). Surface-gravity regime gate (MS only; evolved-giant ω is a v_crit-collapse artifact). Sun/round/endgames byte-identical. **Chunk 2 (frontend-only, 254 pytest unchanged, Playwright 1440+390 zero errors):** an **inclination slider** (0°=pole-on…90°=edge-on, `main.js`-owned `inclinationDeg`, default 60°) driving BOTH the `star.js` tilt θ=90°−i AND `spectrum.js` `v sin i = v_rot·sin i` — **retiring the edge-on-only punt** in the v sin i work. Off the spine (a viewing artifact, never touches the HR marker); a third facet in the unified Rotation control, gated track-stably on `showToggle && rotationOn`. | `whirling-cohort-atlas.md` (Tier C); memory `star-sim-gravity-darkening.md` |
| Magnetic peculiarity (Ap/Bp, magnetic O) | ✅ **done (frontend-only)** | Shipped: an evocative `star.js` surface what-if — co-rotating chemical-abundance spots on an oblique magnetic dipole (the α² CVn look). A `uPeculiar` uniform + `peculiarSpots()` term in `SURFACE_FRAG` (a brightness-only dip, no hue shift — never implies a temperature map), multiplied onto the surface so `uPeculiar=0` (off / out-of-regime / any endgame) is byte-identical. A standalone `#peculiar-toggle` control **track-stable on the A/B-mass regime** (mass ∈ [1.6, 5.0], hidden elsewhere & in endgames — NOT gated on the rotation toggle: Ap/Bp is a magnetic peculiarity, not a rotation-axis effect), with the effect faded **per-state** in `star.js` outside the A/B-MS Teff window (~7–15 kK × a dwarf-gravity gate, so a giant crossing the same Teff stays clean). Labeled evocative (spec §7, the corona precedent). Playwright-verified: visible coherent co-rotating patches on a 3 M☉ B8 V, faded on the EAGB giant, Sun has no control, and it **composes cleanly with gravity darkening** (spots co-rotate over the static gd gradient, no oblate-limb clip) at every inclination. Backend byte-unchanged. | `whirling-cohort-atlas.md` (Tier C) |
| Microturbulence (ξ) | idea | Spectral line-saturation knob (CAP18-large carries it); real but thin pedagogy — likely not worth a control. | `whirling-cohort-atlas.md` (Tier B) |

## Suggested sequencing (synthesis across the plans)

The cheapest-honest-win-first order the individual docs already imply, merged:

1. ~~**Instability-strip HR overlay**~~ — **done** (zero new data, immediate subpopulation payoff).
2. ~~**SED Chunk 3**~~ — **done** (gyrochronology + rotation-slider line collapsing the Chunk-1 band, frontend-only).
3. ~~**Endgame Chunks 4–5**~~ — **done** — the full WR/WD gateway on data already on disk (backend Chunk 1 + frontend Chunks 2 & 3 ✅ + **Chunk 4 ✅** = the WR mode shell + HR + stripped-surface WN→WC→WO composition + WN/WC/WO subtype + **Chunk 5 ✅** = the WR 3D optically-thick-wind shader). **On-hand-data endgame work is complete**; what remains is the data-gated spectra (Chunks 6–7).
4. ~~**Rotation `vvcrit` toggle**~~ — **done (all 4 chunks)** (Chunks 1–3: provider keying + honesty gate + the unified frontend Rotation control with real `v_rot_kms`; **Chunk 4**: fetched the remaining rotating [Fe/H] grids m075/m050/p050 → the rotating axis spans the full −1.0→+0.5 [Fe/H] axis).
5. ~~**SED Chunk 2**~~ — **done** (hot-star wind thermal free–free excess from real Ṁ — the SED's only spine touch; a mid/far-IR→sub-mm feature for evolved hot supergiants, corrected from the plan's wrong ZAMS-O "mm/radio" table). ~~**v sin i broadening**~~ — **done** (client-side Gray-profile convolution of the baked spectrum driven by the real `v_rot_kms`, edge-on; the other visible rotation follow-on).
6. **Endgame Chunks 6–7** + **[α/Fe]** + **spectra density** — the data-gated spectrum work.
7. **The big engines** (binarity → He axis → α-evolution → live solver) — when one feature justifies the lift; binarity is highest-value and highest-cost.
8. **The outward quartet** (`outward-quartet-atlas.md`) — four new axes turning the sim *outward* from the star's interior: ~~**B isochrone/cluster**~~ **✅ done** (MIST v2.5 `.iso`, baked per-[Fe/H] npz; turnoff-dating HR overlay) → ~~**D habitable zone**~~ **✅ done (D1)** (frontend-only, rides the scale bar; Kopparapu 2014 conservative+optimistic band, marches outward, axis auto-widens for giants) → **C asteroseismology** (frontend-only, the échelle diagram) → **A the observer's view** (the capstone: reddening + SVO photometry + an observational CMD, which composes with B into the real Gaia cluster diagram). Cheapest-first; A last because it lands on top of B for free. **Next = Axis C** (frontend-only, no new data), then Axis A (the capstone, real data touch).

Cross-cutting caution (from the atlas): every new axis multiplies the grid and the UI;
prefer **toggles for discrete real axes** and **views/overlays** over yet another slider,
and keep spectrum-only axes ([α/Fe], v sin i) visibly distinct from evolution axes so a
spectrum-panel knob never implies the star's *track* changed.

## Honesty tiering (the project rule, applied to this list)

Every entry is tiered by **what data backs it**, never by what we wish it showed —
the recurring boron-b8 / VO-7400 / invisible-Na lesson. Tier-1/2 items change the real
`StellarState` or the baked spectrum from real grids; Tier-3 engines need new data/physics;
Tier-4 are evocative-only and must be labeled like the corona (spec §7). When building any
of these, the "don't label a non-feature" check comes first: measure that the effect is
*visible and real through the runtime path* before shipping a guide/control for it.
