# Memory index

- [Star Simulator observer's view / CMD (Axis A)](star-sim-observer-cmd.md) — the theory→telescope bridge: `photometry.py` + `/photometry` + `/photometry_track` siblings (distance + CCM89 extinction + synthetic Bessell B/V + Gaia BP mags → observational (B−V, M_V) CMD, composes with Axis B). A1 backend + A2 reddening overlay + **A3 dedicated CMD PANEL BUILT** (`cmd.js` in its own `#observer-panel`, distance/A_V/R_V sliders moved OUT of Controls; marker VISIBLY moves — observed marker/arrow tip = EXACT `/photometry`, dashed "as observed" locus = uniform vector). User-prompted: A2's overlay only changed READOUT TEXT (A_V 0.5→4 = ~0.01% SED pixels). Gate 0: cube IS absolute F_λ (Sun 98%); cube stops 8999 Å → M_V; SVO ZeroPoints (M_V=4.832); JS ccm89==Python to 10 decimals. Living-only. 425 pytest.
- [Star Simulator habitable-zone band (Axis D)](star-sim-habitable-zone.md) — the circumstellar HZ on the true-size scale bar: a pure L+Teff view (`hz.js` compute + `scale.js` draw), Kopparapu 2014 conservative+optimistic band, marching outward as the star brightens, axis auto-widens for giants. D1 BUILT (frontend-only). Two honesty gates: validate coefficients vs solar anchor before drawing; quartic diverges outside 2600–7200 K → skip band entirely. Spectrum enters via Teff; UV/flares out (caption). 412 pytest.
- [Star Simulator isochrone / cluster overlay (Axis B)](star-sim-isochrone-cluster.md) — the transpose of a track: all masses at ONE age, a coeval-cluster locus on the HR panel, the MS turnoff RINGED (the age clock). Sibling `isochrone.py` + `/isochrone` over MIST's PUBLISHED v2.5 `.iso` grid (baked to per-(feh,vvcrit) npz; turnoff = bluest MS-PHASE row not global max Teff). B1 backend + B2 overlay + hosting (`mist-iso-baked-v1`, 10th tag) + **B3 decoupled cluster-age slider ALL BUILT** — the "cluster aging" movie. 412 pytest.
- [Star Simulator coeval-ensemble overlay (BPASS)](star-sim-coeval-ensemble-bpass.md) — the FIRST ENSEMBLE sibling: `bpass.py` + `/population` (SED) + `/population_hrd` (HR panel), pure numpy/stdlib, not even StellarState. A whole coeval population single-star vs +binaries. Chunks 1 (SED magenta ionizing wedge, v2.3) + 2 (HRD number-density magenta binary-only cells, v2.2.1) + 3 (hosting `bpass-baked-v1`) ALL BUILT. **Gotcha both chunks (Playwright caught, not tests): verify the payoff reaches the CANVAS.** 405 pytest.
- [Star Simulator binary-stripped stars](star-sim-binary-stripped.md) — `binary.py` + `/binary` sibling (Götberg 2018 hot He-star, ~70% WR channel). Path (a) Chunks 1–3 + path (b) Chunks 1–4d ALL BUILT (Götberg-snapshot HR reversal / 3D companion / Roche geometry / POSYDON two-body co-evolving movie / free M1/q/P sliders / [Fe/H] picker). Both arcs complete; CE/compact-object tail → [[star-sim-co-hms-rlo]].
- [Star Simulator CO-HMS_RLO compact-object binary](star-sim-co-hms-rlo.md) — sibling `posydon_co.py` + `/co_binary_track` parameterized by `kind` (CO-HMS_RLO H-rich secondary, or CO-HeMS/CO-HeMS_RLO He-star → DCO/GW-progenitor). **PHASE 1 COMPLETE** (Chunks 1a–1c + 2a–2c, full 8-bucket [Fe/H] axis both He grids). DCO classifier (predicted S1 remnant + known S2). Eddington cue gated off unstable_MT + WD + detached (metal-poor artifact reached 505,221×); re-derived ≤3.65× on He grids. 379 pytest. Next = Phase 2 (initial-He/Y axis).
- [Star Simulator α-enhanced (equivalent-Z) overlay](star-sim-alpha-overlay.md) — Phase 3 of `tempered-lineage-inspiral.md` (COMPLETES it), BUILT. Old-population [α/Fe] what-if: at fixed [Fe/H], α raises true Z → cooler/fainter/longer (opposite of He). Sibling `alpha.py` + `/alpha`, MESA-vs-MESA only. Collapsed to a **Z-only change** (Salaris 1993; Zbase moves, unlike Y). LIGHT HR overlay sharing the He slot; `alpha_fe` DERIVED from the Z ratio. 397 pytest.
- [Star Simulator initial-helium (Y) overlay](star-sim-helium-overlay.md) — Phase 2 of `tempered-lineage-inspiral.md`, BUILT. Globular-cluster 2nd-gen what-if: Y≈0.40 vs 0.27 at matched mass/[Fe/H] → hotter/brighter/shorter. Sibling `helium.py` + `/helium`. Claude ran the 6-run MESA batch in Docker (Zbase stays 0.0152 — Y leaves Z fixed). LIGHT HR overlay, **MIST spine hidden** (MESA-vs-MESA never MESA-vs-MIST); τ_MS in the caption. 387 pytest.
- [Star Simulator hosted data assets](star-sim-hosted-data-assets.md) — pre-baked GitHub Release assets (10 tags): POSYDON 8 buckets, MIST tracks + isochrones (`mist-iso-baked-v1`), Koester+TMAP, PoWR, Coelho, Gotberg, self-run helium/alpha MESA, BPASS. "MESA excluded" is ONLY third-party bearums validation tracks — SELF-RUN MESA IS hosted. Recurring hazard: adding buckets exposes latent `_available_grids()[0]`/hardcoded-feh test bugs.
- [Star Simulator init scope](star-sim-init-scope.md) — what init delivered: the §3 provider spine, stub limits (historical).
- [Star Simulator MISTProvider](star-sim-mist-provider.md) — live provider: MIST v2.5, 2D (mass×[Fe/H]) EEP interpolation, blend-then-invert, non-rectangular domain, ZAMS→EAGB window (TPAGB hard-stopped), full 0.1–300 M☉ grid, .npz parse cache. Phase 1.
- [Star Simulator composition panel](star-sim-composition-panel.md) — §5.4 panel + `/track` endpoint: EEP-axis stacked areas, track returns list[StellarState], shared `_state_from_row`.
- [Star Simulator frontend UX](star-sim-frontend-ux.md) — single-source age window, snap-tick strips + editable inputs, `?`-glyph hover pedagogy, `canvas.js` fitCanvas; + 2026-07 visual passes (Teff-colored track/marker/spectral bands, past/future split, swallowed-orbit rings, starfield, skeletons, iso-radius HR diagonals, comp boundary dots, header "ZAMS → remnant"); + reserved Controls-panel height (mass-gated what-ifs present-but-greyed with "Appears for…" notes via `reserveWhatIf`, rotation section min-height — no age-slider shove on a mass drag; `advisor`'s 3-hide-reasons rule reverses "absent, not a dead knob" for regime-gating only); + WITHIN-panel reservation (4th anti-jump pass, desktop-only): `.rot-control` min-height→316 (its tallest state — kills the incl-reveal-on-check + mass-regime reflow) + per-note floors (`#alpha-track-note` 80 etc.) so an element appearing never shoves its siblings; user chose full-reserve-with-gap; verify with SCROLL-INVARIANT panel-relative tops, run Playwright serially.
- [Star Simulator draggable/responsive panels](star-sim-draggable-responsive-panels.md) — dashboard UX: reorder-in-flow sortable (`layout.js`), responsive canvases (`resize`+ResizeObserver, the fitCanvas inline-width gotcha), touch/auto-scroll; + density pass. Frontend-only.
- [Star Simulator UX four fixes](star-sim-ux-four-fixes.md) — frontend bundle: `<input type=range>` quantization → source-of-truth `massValue`/`ageValue`/`gyrNum`; SED X-ray "gap" marker; toggle-without-resize (`visibility:hidden`); `classify.js` MK type; X-ray morph not snap.
- [Star Simulator age-tick fixes](star-sim-ux-age-tick-fixes.md) — five age-slider landmark-tick bugs: range-step quantization off-by-one phase, RGB-tip≈CHeB-onset dedup, endpoint snap targets, chain-stagger/edge-anchor labels, phone-sized MIN_GAP. Frontend-only.
- [Star Simulator nine UX fixes](star-sim-ux-nine-fixes.md) — third user-reported-fix batch (A→E) ALL BUILT: HR auto-fit framing; endgame quick-wins; readout own panel + always-greyed gateway; gateway latency-not-logic fix + type-only `/endgame?meta=1` fast-path; age-slider remap → linear-in-EEP (marker PICKED from /track by row, 58.5% post-MS travel). Plan `docs/plans/polished-cinder-frame.md`.
- [Star Simulator instability-strip overlay](star-sim-instability-strip-overlay.md) — opt-in "Variable-star zones" HR overlay (`hr.setOverlay`): classical strip + LBV + Mira, labeled schematic; first "show a subpopulation" feature. Frontend-only.
- [Star Simulator true-size scale bar](star-sim-true-size-scale-bar.md) — 3D star +20% (tangent-cone clip math) + true-size log scale bar (`scale.js`, Earth/Sun/planet-orbit landmarks). Frontend-only.
- [Star Simulator tooltip singleton](star-sim-tooltip-singleton.md) — one body-mounted `position:fixed` tooltip layer (`tooltip.js`, delegated on `[data-tip]`, JS-clamped both axes) replacing viewport-clipping CSS `::after` hovers. Frontend-only.
- [Star Simulator Phase 2 shaders](star-sim-phase2-shaders.md) — §7 beauty: Planck→CIE color, H_p Worley granulation, limb darkening, streak-proof rotation, corona quad. + granulation moiré/grid fix (hash33 swap + per-octave fwidth antialiasing). Phase 2.
- [Star Simulator Phase 3 Lane–Emden](star-sim-phase3-lane-emden.md) — §8 static polytrope panel: sibling not provider, `/polytrope` bypasses PROVIDER, DOP853+θ=0 event, closed-form + Chandrasekhar validation. Phase 3.
- [Star Simulator Phase 4 CNO](star-sim-phase4-cno.md) — per-element composition: `metals_surf`/`metals_core` dicts (state.py untouched), MIST isotope-summed threading through sixteen elements (cache v2→v8); three comp.js views incl. log-scale + light panel. Gotchas: Fe diffusion, Na/Li not inert, boron radioactive, Cr/Mn/Ni absent, verify off-grid feh blend. Phase 4.
- [Star Simulator Phase 4 MESAProvider](star-sim-phase4-mesa.md) — second real provider (MESA history.data), discrete-grid multi-Z-by-snapping, no cross-interp; measured MESA-vs-MIST cross-val matched on Z+Xc; self-run solar bucket via Docker MESA. Opt-in.
- [Always commit and push](always-commit-push.md) — standing preference: finish substantive changes by committing AND pushing unprompted.
- [Session-end ritual](session-end-ritual.md) — at batch/session end: update memory + docs, commit, push.
- [Star Simulator GitHub repo](star-sim-github-repo.md) — public repo location + how it was created.
- [Star Simulator non-thermal SED plan](star-sim-nonthermal-sed-plan.md) — coronal X-ray + Güdel–Benz radio overlay on the blackbody SED floor: ALL 3 chunks BUILT (band; rotation→X-ray line; hot-star wind free–free excess from real Ṁ — the spine touch `mdot_msun_yr` on StellarState). Honesty-gate band-not-fake-line; Chunk 2 corrected to evolved-supergiant mid/far-IR (ζ Pup anchor). Plan `docs/plans/magnetic-ember-broadcast.md`.
- [Star Simulator WR/WD endgame plan](star-sim-wr-wd-endgame-plan.md) — Wolf–Rayet & white-dwarf endgame via a reversible gateway, Chunks 1–7 ALL BUILT (arc COMPLETE): backend endgame()+classifier; WD scrubber/shader/structure; WR mode + HR-to-316kK + WN/WC/WO subtype; WR wind 3D shader; Koester DA WD spectra; TMAP hot-WD/CSPN splice; PoWR WR spectra (narrow-GO). No new provider (endgame IS StellarState), snap-to-track. Plan `docs/plans/smoldering-cinder-gateway.md`.
- [Star Simulator SN + NS/BH endgame](star-sim-supernova-remnant-endgame.md) — core-collapse SN arc COMPLETE (Chunks 0–5, 220 pytest): fills the dead `type="SN"` branch. Hybrid sibling (classify in PROVIDER.endgame()+scalars; compute in pure `supernova.py`+`/supernova`). ⁵⁶Ni curve (Arnett Co-tail + Popov/KW IIP plateau) + ejecta expansion + NS/BH/failed-SN remnant. 3D fireball→remnant; pre-collapse onion; fallback continuum `φ=smoothstep(CO 7→12)`, NS↔BH flips at remnant ~2.5 M☉. Type II only (not Ia). Plan `radioactive-afterglow-requiem.md`.
- [Star Simulator rotation & subpopulation atlas](star-sim-rotation-subpop-atlas.md) — rotation `vvcrit` axis, Chunks 1–4 ALL BUILT: provider keys by `(feh,vvcrit)` + snaps buckets; `rotation_status` gate; API `vvcrit` param + real `v_rot_kms` (`CACHE_VERSION`→11) + unified Rotation control; full −1.0→+0.5 [Fe/H]. + Tier-B v sin i broadening + [α/Fe] spectral axis (Coelho-2014, `#alpha-toggle` spectrum what-if) BUILT. 254 pytest. Plan `docs/plans/whirling-cohort-atlas.md`.
- [Star Simulator roadmap (future-work index)](../plans/ROADMAP.md) — canonical cross-plan index of everything proposed-but-unbuilt (SED/endgame/atlas/spectra-density), grouped by readiness. Update this, not a second list, when scope changes.
- [Star Simulator Ap/Bp peculiarity](star-sim-apbp-peculiarity.md) — evocative Tier-C 3D what-if (frontend-only, backend byte-unchanged): co-rotating chemical-abundance spots on an oblique magnetic dipole (α² CVn). `star.js` `uPeculiar`+`peculiarSpots()` brightness-only dip (GLSL: `patch` reserved → `mott`); `#peculiar-toggle` track-stable on A/B mass [1.6,5.0], faded outside the ~7–15 kK×logg window; composes with gravity darkening. Byte-identical when off.
- [Star Simulator gravity darkening](star-sim-gravity-darkening.md) — oblateness/gravity-darkening of fast rotators (capstone to the vvcrit axis): Gate 0 PASSED (real vvcrit=0.4 data, subtle). Real ω (v_crit Roche), OFF the spine. Chunks 1 & 2 BUILT (frontend-only): oblate geom + pole-hot/equator-cool gradient; inclination slider (main.js, 60° default) driving star tilt θ=90°−i AND spectrum v sin i (retires the edge-on punt).
- [Star Simulator Phase 5 spectra](star-sim-phase5-spectra.md) — synthetic-spectrum panel DONE: MSG/pymsg build-time bake → scipy runtime `spectra.py` serving `/spectrum` (sibling, bypasses PROVIDER); 3-grid Teff 2300–55000 K cube, axis-generic; + broadband SED panel; + rainbow far-red CMF-clamp fix. 121 tests. Recipe `backend/docs/msg_spectra_build_recipe.md`.
- [Star Simulator real interior structure (MESA)](star-sim-interior-structure-mesa.md) — the honest Lane–Emden successor, BUILT: `structure.py` sibling + `/structure` serving a REAL MESA `profile.data` snapshot — ρ/T/X(r) + the true convective/radiative split — snapped (mass,[Fe/H],age) to nearest, + two CANONICAL polytrope overlays (n=1.5/3, NOT fitted). Regimes: 0.25 M☉ fully-convective · 0.8/1 M☉ radiative-core+conv-envelope · 1.3 M☉ three-layer · 2/6/15/25 M☉ convective-core FLIP. Partial 2D (mass×[Fe/H]) grid at 0.8+1 M☉. 239 pytest.
