# Memory index

- [Star Simulator init scope](star-sim-init-scope.md) — what init delivered: the §3 provider spine, stub limits (historical).
- [Star Simulator MISTProvider](star-sim-mist-provider.md) — live provider: MIST v2.5, 2D (mass×[Fe/H]) EEP interpolation, blend-then-invert, non-rectangular domain, ZAMS→EAGB window (TPAGB hard-stopped), full 0.1–300 M☉ grid, .npz parse cache. Phase 1.
- [Star Simulator composition panel](star-sim-composition-panel.md) — §5.4 panel + `/track` endpoint: EEP-axis stacked areas, track returns list[StellarState], shared `_state_from_row`.
- [Star Simulator frontend UX](star-sim-frontend-ux.md) — single-source age window (track as driver), snap-tick strips + editable inputs, `?`-glyph hover pedagogy, `canvas.js` fitCanvas; + the 2026-07 visual passes: Teff-colored living track + glowing marker + spectral-class bands, past/future track split, swallowed-orbit rings, starfield backdrop, first-load skeletons, iso-radius HR diagonals (WD cooling slides down one), comp boundary dots, tick/SED label legibility, header tag "ZAMS → remnant".
- [Star Simulator draggable/responsive panels](star-sim-draggable-responsive-panels.md) — dashboard UX: reorder-in-flow sortable (`layout.js`), responsive canvases (`resize`+ResizeObserver, the fitCanvas inline-width gotcha), touch/auto-scroll; + density pass (readout auto-fit, per-element legend toggle). Frontend-only.
- [Star Simulator UX four fixes](star-sim-ux-four-fixes.md) — frontend bundle: `<input type=range>` quantization → source-of-truth `massValue`/`ageValue`/`gyrNum`; SED X-ray "gap" marker; toggle-without-resize (`visibility:hidden`); `classify.js` MK type (phase-first luminosity class); X-ray morph not snap.
- [Star Simulator age-tick fixes](star-sim-ux-age-tick-fixes.md) — five age-slider landmark-tick bugs: range-step quantization off-by-one phase, RGB-tip≈CHeB-onset dedup, endpoint snap targets, chain-stagger/edge-anchor labels, phone-sized MIN_GAP. Frontend-only.
- [Star Simulator nine UX fixes](star-sim-ux-nine-fixes.md) — third user-reported-fix batch (A→E), **ALL FIVE BUILT**: A = HR auto-fit framing (endgame `fitBounds`, hybrid living frame, WD-preview leader); B = endgame quick-wins (WR SED caption branches on `endgameWR`; Lane–Emden-for-WR = no-fix; yellow `.endgame-back`); C = readout split into own panel + gateway button **always-placed-greyed-until-end** (not empty-reserve; SN note foreshadows from ZAMS, user-confirmed); D = gateway auto-appear was **latency not logic** → "computing the star's fate…" placeholder (in-flight-flag-gated) + a safe de-dup (+474 vs +628 ms) + resnap note moved below sliders + the **type-only `/endgame?meta=1` fast-path** (additive `fetchEndgameMeta` + separate cache STRUCTURALLY decouples the button from the 1 MB; advisor-caught enable-gating `disabled=!atEnd||!eg`); E = **age-slider remap → linear-in-EEP** (item 2): the marker is now **PICKED from the `/track` by row, no `/state` fetch** (like WD/WR scrubs), killing the dead MS plateau — mass-invariant **58.5% post-MS** travel (track = a fixed 606 rows, MS 42/RGB 25/CHeB 17/EAGB 17% at every mass). Advisor's two decisions, measured: (1) marker MUST come from the EEP coordinate not an age round-trip (flat-age bands — the 1 M☉ CHeB blue loop = 48 rows over ~4800 yr — would be degenerate under `/state?age=`); (2) linear-in-EEP (lockstep with `comp.js` `xOf(eep)`), not piecewise. Simplifies: landmarks by row index, `ageValue` a derived readout, off-by-one obsoleted; `/state` kept-but-unused. Verified in-app: dead bands animate, Sun on §10 anchor under nearest-row (no scalar-interp). 138 pytest (backend untouched). Plan `docs/plans/polished-cinder-frame.md`.
- [Star Simulator instability-strip overlay](star-sim-instability-strip-overlay.md) — opt-in "Variable-star zones" HR overlay (`hr.setOverlay`): classical strip + LBV + Mira, labeled schematic; first "show a subpopulation" feature. Frontend-only.
- [Star Simulator true-size scale bar](star-sim-true-size-scale-bar.md) — 3D star +20% (tangent-cone clip math) + true-size log scale bar (`scale.js`, Earth/Sun/planet-orbit landmarks). Frontend-only.
- [Star Simulator tooltip singleton](star-sim-tooltip-singleton.md) — one body-mounted `position:fixed` tooltip layer (`tooltip.js`, delegated on `[data-tip]`, JS-clamped both axes) replacing viewport-clipping CSS `::after` hovers. Frontend-only.
- [Star Simulator Phase 2 shaders](star-sim-phase2-shaders.md) — §7 beauty: Planck→CIE color, H_p Worley granulation, limb darkening, streak-proof rotation, corona quad. + granulation moiré/grid fix (hash33 swap + per-octave fwidth antialiasing). Phase 2.
- [Star Simulator Phase 3 Lane–Emden](star-sim-phase3-lane-emden.md) — §8 static polytrope panel: sibling not provider, `/polytrope` bypasses PROVIDER, DOP853+θ=0 event, closed-form + Chandrasekhar validation. Phase 3.
- [Star Simulator Phase 4 CNO](star-sim-phase4-cno.md) — per-element composition: `metals_surf`/`metals_core` dicts (state.py untouched), MIST isotope-summed threading through sixteen elements (cache v2→v8); three comp.js views incl. log-scale + light panel. Gotchas: Fe diffusion, Na/Li not inert, boron radioactive, Cr/Mn/Ni absent, verify off-grid feh blend, Playwright headless. Phase 4.
- [Star Simulator Phase 4 MESAProvider](star-sim-phase4-mesa.md) — second real provider (MESA history.data), discrete-grid multi-Z-by-snapping, no cross-interp; measured MESA-vs-MIST cross-val matched on Z+Xc; self-run solar bucket via Docker MESA. Opt-in.
- [Always commit and push](always-commit-push.md) — standing preference: finish substantive changes by committing AND pushing unprompted.
- [Session-end ritual](session-end-ritual.md) — at batch/session end: update memory + docs, commit, push.
- [Star Simulator GitHub repo](star-sim-github-repo.md) — public repo location + how it was created.
- [Star Simulator non-thermal SED plan](star-sim-nonthermal-sed-plan.md) — coronal X-ray + Güdel–Benz radio overlay on the blackbody SED floor: **ALL 3 chunks BUILT** — 1 (band), 3 (rotation→X-ray line), 2 (hot-star wind thermal free–free excess from real Ṁ — the spine touch: `mdot_msun_yr` on StellarState). Honesty-gate band-not-fake-line. **Correction:** Chunk 2 is a mid/far-IR→sub-mm excess for EVOLVED hot supergiants, NOT ZAMS-O mm/radio (plan table was ~4 dex too bright/circular; fixed via ζ Pup anchor + proven BB norm). Plan `docs/plans/magnetic-ember-broadcast.md`.
- [Star Simulator WR/WD endgame plan](star-sim-wr-wd-endgame-plan.md) — Wolf–Rayet & white-dwarf endgame via a reversible gateway: Chunks 1 (backend `endgame()`+classifier), 2–3 (WD scrubber/shader/structure + continuous transition + Lane–Emden hint), 4 (WR mode shell + HR-to-316kK + stripped-surface WN→WC→WO via the normal comp views + subtype + re-snap) & 5 (WR optically-thick-wind 3D shader: limb-brightened electron-scattering haze + outward-advected value-noise filaments, Z_surf density cue WN→WC/WO, honest-Teff-color/no-chemistry-hue, gentle clamped-L intensity not a measured Ṁ, fit-to-frame extent for the huge R≈33 entry, uTime wiring) & 6a (Koester DA WD spectra: a SECOND spectrum sibling `/wd_spectrum`, separate rectangular host-baked Teff×logg cube — 82×13, pure-H/no [Fe/H], no Docker/pymsg; DC Planck below the ~5000 K floor + 80 kK no-model honesty edges; phase-aware logg≥6.0 cube switch; BAKE_VERSION across THREE files; 153 tests) & 6b (TMAP hot-WD/CSPN spectra: SVO `tmap` H-rich Hemass=0 slab spliced as the >80 kK hot slab of the SAME WD cube → 93×13, grid `koester2-DA+TMAP-CSPN`, host-baked `fetch_tmap.py`; `regime="CSPN"` (logg-aware: 55–80 kK low-g rise reads CSPN, hot high-g remnant stays DA), `refreshWD` routes logg≥6 OR Teff>55 kK here (the MAIN cube's OSTAR ceiling — advisor-caught: at >80 kK the contracting rise flashed "no model"); **measured corrections to the §7 scoping: NO ×π×10⁸ — SVO ascii is physical, seam ratio 1.005–1.021 → no rescale; vacuum→air; log-linear blue-gap fill; logg-clamp honest (optical Δ0.03 vs 0.41 DA); NO BAKE_VERSION bump (OSTAR precedent, re-bake before tests); Query teff bound→500000; residual no-model re-pointed at 190 kK**; 157 tests) BUILT & 7 (WR spectra, PoWR — **narrow-GO**, the LAST chunk → endgame arc COMPLETE: a THIRD `/wr_spectrum` sibling keyed on the WR axes (T\*, Rt) NOT (Teff, logg), host-baked `fetch_powr.py`+`bake_wr_spectra.py` FLAT-NODE cube (ragged (T\*,Rt) parallelogram, no rectangular void-fill), gal/lmc/smc grids; the 7a MEASURED gate → narrow-GO: PoWR covers only the cool H-rich WNh entry (~9%), the stripped core is the hot/compact EVOLUTIONARY surface (Teff 150–262 kK ≈ T\*, the evolutionary-vs-spectroscopic Teff gap) → honest no-model frame; map Teff→T\* DIRECTLY (advisor REVERSED on my locus evidence — Teff→T_2/3 would clamp everything), Rt from a Nugis–Lamers Ṁ (star_mdot stays OFF StellarState); `spectrum.js updateWR` = EMISSION draw (continuum-normalized, lines UP, species guides); 170 tests) BUILT. No new provider (endgame IS StellarState), snap-to-track. Plan `docs/plans/smoldering-cinder-gateway.md`, spectra recipe `backend/docs/msg_spectra_build_recipe.md §7a/§8/§8b/§9`.
- [Star Simulator SN + NS/BH endgame](star-sim-supernova-remnant-endgame.md) — Core-collapse SN arc — **COMPLETE: Chunk 0 gate DONE + Chunk 1 (backend) + Chunk 2 (frontend) + Chunk 3 (3D fireball→remnant) + Chunk 4 (pre-collapse onion shell) + Chunk 5 (remnant branch NS/BH/failed-SN + cliff softening) all BUILT** (220 pytest; plan `radioactive-afterglow-requiem.md`): fills the dead `type="SN"` branch with a ⁵⁶Ni light curve (Arnett Co-tail + Popov/KW IIP plateau) + ejecta expansion + NS/BH/failed-SN remnant. **Hybrid sibling** (classify stays in `PROVIDER.endgame()`+progenitor scalars; compute in a new pure `supernova.py`+`/supernova` emitting StellarStates AND a light-curve object). Chunk 1: `EndgameResult` scalars `pre_sn_radius_rsun`/`he_core_msun`/`co_core_msun`/`h_retained` (SN-branch only), `CACHE_VERSION` 11→12 (he/c/o core cols, Mcur-pattern), **R₀ = max over final-phase rows excl. terminal artifact** (the open knob, settled), light curve `w·L_p·rise+(1−w)·L_radio`, NS/BH CO-cut, no-plateau fallback (compact low-Z R₀<300). **Two advisor test corrections (measurement-validated):** Tier-3 scales the radioactive **TAIL** not the peak (IIP peak = the M_Ni-free plateau, so "peak∝M_Ni" would go red); Tier-1 Co-slope measured on the served **`L_radio` component** (0.00976) not `L_total` (0.01023, plateau bleed). **Chunk 2 (frontend):** a reversible `sn-mode` mirroring WD/WR — the dead SN note → a `→ Continue: Supernova` gateway button; the HR panel CSS-swaps its title to **Supernova light curve** and `hr.setSupernova()` redraws it as **L vs LINEAR days** (the straight ⁵⁶Co tail — log-time would bend it); the age slider → a linear-days scrubber (`snStateIndex`); the ⁵⁶Ni slider (`sn-control`, Tier-3, debounced refetch) lifts the **tail not the plateau**; the **cited observed-photometry overlays** (`frontend/src/sn.js` — SN 1987A tail + SN 1999em IIP plateau, **published bolometric *fits*, not raw photometry** — CDS/VizieR all failed; cited in code AND a visible `.hr-sn-caption`) are the **deferred Tier-1 anchor** (constraint #5 met). Consumers get the photosphere StellarState via an explicit `{endgame:"sn"}` signal (NOT naive reuse — `logg≈−5` ejecta would trip the boiling-fireball gate); `comp.js`/`spectrum.js` honest placeholders, entry narrates the un-modeled bounce. Playwright-verified 1440+390 px. **Chunk 3 (frontend-only, 3D fireball→remnant):** `star.js` `FIREBALL_FRAG` (additive `fireball` sphere reusing `star.geometry`, 3D value-noise fbm on a **bounded** time orbit so it boils with no drift/seam, center-bright, color = the **honest blackbody Teff** — blue-white→orange→red IS the cooling, the one load-bearing cue; a single back-face-culled additive layer with clamped alpha keeps the hue + cell structure ⇒ **no white-out**) + `REMNANT_FRAG` (a small camera-facing additive dot). `star.visible=!sn` set **unconditionally** (mode switch can't strand a mesh). `refreshSN` drives `snGrow`=`0.4+0.6·smoothstep01(0,0.14,dayFrac)` (early "expanding" beat — on-screen size decoupled from true AU-scale R, on the scale bar) + `snFade`=`smoothstep01(0.55,1,dayFrac)` (late dissipation), threaded `{endgame:"sn", remnant, snGrow, snFade}`. As `snFade`→1 the remnant emerges: **NS** → a tiny hot blue-white dot (color **labeled evocative, NOT a Teff** — real NS optical thermal emission negligible, Crab V≈16 synchrotron); **BH/failed** → no dot, the frame goes dark ("**winks out**"). **Measured (advisor: confirm BH-on-SN exists before treating winks-out as tested):** probed the real provider → BH-on-SN IS in-grid at **30 M☉ solar** (CO 7.68 > 7.0 cut) vs **15 M☉ solar** NS — both verified on-screen across explosion/plateau/late-reveal. The screenshots ARE the check (a default zero-console pass would go green on dead reveal logic). **3-tier honesty** (⁵⁶Co tail slope bulletproof / plateau shape from MIST M_ej,R₀ / peak∝M_Ni free slider). Gate: **SN bucket purely Type II** (Ib/c = the WR endpoint → follow-on); canonical 15 M☉ in regime (plateau 1.83e42/138.7 d). NO explosion mechanism; observed light curves verify. **Chunk 4 (frontend-only, pre-collapse onion shell):** `comp.js` `drawSNOnion` replaces the SN placeholder with the progenitor's onion cross-section from the scalars `/supernova` already serves (no backend change); real boundaries (remnant/CO/He/total) sized **area∝mass**, inner Si/Fe **faint schematic** (MIST v2.5 ends before the iron core forms — caption owns it), **NS/BH contrast honest** (BH swallows the CO core → onion visibly LACKS the copper band, only He+H eject), **⁵⁶Ni an exaggerated slider-tied ring** (kept on BH too, consistent with the curve). Static across the scrub (`comp.update` no-op in SN mode), redraws on ⁵⁶Ni-slider/mass-re-snap (both verified live). Phone-width label-overflow bug caught+fixed (even-spacing, caveats fold into the caption). The CO=7.0 cut is now a visual cliff in TWO places (light curve + onion) → Chunk 5 softens both. **Chunk 5 (remnant branch + cliff softening; backend + frontend, +5 SN tests = 220 pytest, Playwright-verified):** the hard CO=7 cut → a **labeled fallback continuum** `φ=smoothstep(CO_NS_MAX=7, CO_DIRECT=12, co_core)` that softens the cliff in BOTH the light curve and the onion at once — remnant grows `M_NS+(M_final−M_NS)·φ`, ejecta `M_ej=M_final−M_remnant` shrink to ~0, **ejected** ⁵⁶Ni `=M_Ni·(1−φ)` dims (deepest ash falls back first). **Advisor blocker 1:** the **NS↔BH label flips at the remnant crossing NS_MAX~2.5 M☉, NOT at CO=7** (else a mass-gap "BH" reads as a bug) → **30 M☉ solar is now a heavy NS ~2.16, not a BH** (new demos: BH-fallback 40 M☉/[Fe/H]−0.5, failed 50 M☉/[Fe/H]−1.0). **Advisor blocker 2:** a **failed SN does NOT reuse homologous expansion** (`v=√(2E/M_ej)` blows up, ejected Ni→0 → log(0)) → it stays at R₀ and DIMS (the "disappearing supergiant" N6946-BH1); served curve uses a tiny floored Ni (positive, ~3 dex below a real IIP). **Measured reachability:** SN-bucket CO core caps ~14.2 (50 M☉/[Fe/H]−1.0; heavier→WR) so CO_DIRECT=12 makes failed reachable. **3D reconciled (a Chunk-3 inconsistency fixed — Chunk 3 made all BH wink out):** NS→bright fireball+dot, **BH-fallback→bright fireball→fades to a dark invisible remnant (no dot, NOT a wink-out)**, **failed→dim grey→black (winks out)**. New served fields `fallback_fraction`/`failed_sn`/`m_ni_ejected_msun`; onion drops the rem≤CO clamp (remnant eats C/O→He→H), ring uses ejected Ni, failed onion ≈ all void; `hr.js` violet failed-SN banner; readout shows "% fell back" + "direct collapse"; classify/SED branch on failed. **The SN arc (Chunks 0–5) is COMPLETE.** Type Ia is NOT this arc (binarity §3, off the WD branch).
- [Star Simulator rotation & subpopulation atlas](star-sim-rotation-subpop-atlas.md) — rotation `vvcrit` axis: gate SETTLED (mass-ramped at ~1.2 M☉ Kraft break), **Chunks 1–4 ALL BUILT — arc complete**; + **Tier-B v sin i line broadening BUILT** (client-side Gray-profile convolution in `spectrum.js`, driven by real `v_rot_kms` edge-on, main-cube only, no backend touch — advisor settled against an inclination slider) (1: provider keys by `(feh,vvcrit)`, snaps between buckets; 2: data-derived `rotation_status` honesty gate; 3: API `vvcrit` param + `/rotation_status` route + real `v_rot_kms` from `surf_avg_v_rot` (`CACHE_VERSION`→11) + a **unified frontend "Rotation" control** with two regime-gated facets — vvcrit track toggle + the relocated SED period slider; 4: fetched m075/m050/p050 vvcrit0.4 → rotating axis spans the **full −1.0→+0.5 [Fe/H] axis**, `has_grid` honest across it, two stale "incomplete-axis" tests flipped); + binarity/[α/Fe] atlas, honesty tiers A–D, "nothing is out of scope." Plan `docs/plans/whirling-cohort-atlas.md`.
- [Star Simulator roadmap (future-work index)](../plans/ROADMAP.md) — canonical cross-plan index of everything proposed-but-unbuilt (SED/endgame/atlas/spectra-density), grouped by readiness. Update this, not a second list, when scope changes.
- [Star Simulator Phase 5 spectra](star-sim-phase5-spectra.md) — synthetic-spectrum panel DONE: MSG/pymsg build-time bake → scipy runtime `spectra.py` serving `/spectrum` (sibling, bypasses PROVIDER); 3-grid Teff 2300–55000 K cube, axis-generic; + broadband SED panel; + rainbow far-red CMF-clamp fix. 121 tests. Recipe `backend/docs/msg_spectra_build_recipe.md`.
- [Star Simulator real interior structure (MESA)](star-sim-interior-structure-mesa.md) — the honest Lane–Emden successor, BUILT (the **four mass regimes** + the **metallicity axis**: 1 M☉ at [Fe/H]=−1/+0.5, the convective envelope shallows monotonically as Z drops — base 0.70/0.75/0.95 at +0.5/0/−1, matched-Xc-measured, NOT a core flip (expected_n stays 3), −2.0 run but not shipped (fragments); MESA gotcha: change Zbase too; runtime no-change drop-in, +requires_structure_multifeh marker +1 test +[Fe/H]-snapped-far note, 237 pytest): **0.25 M☉ fully-convective M dwarf** + 1 M☉ solar radiative-core+conv-envelope + the **2/6 M☉ convective-core↔radiative-envelope FLIP** + the **15 M☉ SN-progenitor slice** + the **25 M☉ upper-SN-range bracket**, 236 pytest): `structure.py` sibling + `/structure` (bypasses PROVIDER) serving a REAL MESA radial profile.data snapshot — ρ(r)/T(r)/X(r) + the true convective/radiative split (`mixing_type==1`∪Schwarzschild) — snapped (mass,[Fe/H],age) to the nearest saved snapshot, + the two CANONICAL polytrope overlays (n=1.5/3, NOT fitted — advisor-settled, the departure is the lesson). Own MESA-profile parser (never imports a provider — §3). MESA-only/offline (MIST has no profiles, live solve out of scope §2/§9): self-run once via Docker MESA (solar recipe + profile snapshots, `backend/docs/mesa_structure_recipe.md`), gitignored `data/mesa_profiles/`. Frontend `structure.js` = live consumer wired into paintState, jumps between snapshots, snapped-far note off-grid. Measured mid-MS: 1 M☉ ρ_c≈190/T_c≈1.66e7/radiative-core+conv-envelope base r/R≈0.75; **6 M☉ ρ_c≈16/T_c≈2.95e7/convective-core 0→0.131/radiative envelope → expected_n flips to 3/2** (2 M☉ likewise, core 0→0.088); near-ZAMS 1 M☉ transient convective core verified vs raw column. **2/6 M☉ = NO runtime code change** (index globs tree); added `requires_structure_massive` marker + 3 flip tests + fixed 1 stale off-grid-snap test; advisor semiconvection-OR caveat re-checked → doesn't bite (OR adds nothing beyond mixing_type here). 14 structure tests (234 pytest), Playwright 1440 (skipped for the data-only 25 M☉ drop-in — payload verified via the real `interior_structure()` path instead). **25 M☉** = same recipe, `initial_mass=25.0`, profiles 9–13, mid-MS anchor profile11 (Xc 0.465): ρ_c≈3.79/T_c≈3.78e7/R≈8.47/**core 0→0.228** (the deepest, 0.131→0.178→0.228), threshold set from measurement (>0.20); OR-clause re-checked (zero mid-radius over-shade). Snapshot-selection is the risk (massive core shrinks toward TAMS → kept a healthy mid-MS Xc≈0.4–0.46 anchor). **0.25 M☉** = the third regime, a **fully-convective M dwarf** (advisor chose it over the literal "Next: other-Z" — a new *regime* is visible-by-construction, honesty gate met automatically): single convective zone **0→1** (whole star), `expected_n`→3/2, ρ_c≈135–138/T_c≈7.4e6 (below Sun's 1.5e7, weak pp)/R≈0.247. Two advisor-flagged run changes: **max_age=2d9 replaces the TAMS stop** (MS lifetime ~10¹²⁻¹³ yr, Xc barely moves) + **ship a settled-MS profile not a pre-MS-contracting one** (profiles 20/21/22 of 22, L settled ~0.0105 L☉). **The polytrope-honesty INVERSION** (measured first): the real ρ **hugs n=1.5** (~1–5%), far above n=3 — the one bucket where the idealization *works*, inverting "the departure is the lesson." NOT a pure drop-in: +`requires_structure_lowmass` marker (≤0.5), +2 tests, off-grid snap probe 0.3→0.15 (0.25 is the new floor), + a small `structure.js` caption refinement ("fully convective", blank conv.base). Playwright-verified 1440 (ρ overlaps n=1.5 dash, whole-panel convective shading, zero errors). **The [Fe/H] axis now lives at a SECOND mass — 0.8 M☉ K dwarf** (recipe §11) → first genuine **partial 2D (mass×[Fe/H]) grid**: same regime as the Sun but deeper envelope, base 0.66/0.69/0.81 at [Fe/H] +0.5/0/−1 (matched-Xc, monotone, UNFRAGMENTED, core radiative n=3). Chosen by measurement over two rejects: **6 M☉ convective-core edge vs Z FAILED** (~0.02 shift, non-monotone below Xc 0.5 — massive stars respond to Z in R/Teff not core fraction, advisor predicted); **1.3 M☉ envelope** clean 2-point but fragments into ~0.99 slivers by [Fe/H]=−0.5 (real mixing_type, split-mask-checked; its double-convective core+envelope structure is a possible standalone slice). Run changes: **max_age=2d10** (K-dwarf MS ~25 Gyr) + drop pre-MS Hayashi profiles. Runtime NO code change (mass→feh→age snap; Z-less mass→solar fallback, both verified via real path — closes advisor's partial-2D-topology concern); +3 dirs + 1 test (mass-parametrized `_midms_envelope_base`, existing multifeh marker) + structure.js comment refresh. 238 pytest, Playwright 1440 (conv. band shallows 0.659→0.694→0.807, zero errors). **[Fe/H]-axis clean window now MEASURED to its edges → closed (recipe §12):** 0.6 M☉ non-ship (spread ~0.09<0.1, +0.5≈0.0 — compression, deeper envelope gives SMALLER Z-response) + 1.1 M☉ non-ship (spread big but −1.0 fragments to 2 zones + core type flips conv/rad/conv across Z); 0.9 clean but redundant. Usable window = **0.8–1.0 M☉, fully covered**; no data/test/code shipped (negative result, docs only). **The 1.3 M☉ transitional double-convective slice now BUILT (recipe §13) — the FOURTH regime:** a convective core AND a convective envelope at once (the **three-layer sandwich** conv/**rad gap**/conv), the bridge between the Sun and the massive stars. Drop-in bucket (initial_mass=1.3, solar Z, **normal TAMS stop** — 3.1 Gyr MS), ships **4 MS snapshots** (`solar_1p3Msun/` profiles 8/9/10/11; anchor p10 ρ_c≈156/T_c≈2.11e7/R≈1.66/core 0→0.059/**envelope base 0.885**); at TAMS the core vanishes → not shipped. **Advisor trap avoided:** the caption's DEEP-envelope gate `z[0]<0.95` excludes the 6/15/25 M☉ sub-surface sliver (r/R≈0.994) so they aren't relabeled double-convective (verified via real path + Playwright: 6 M☉ caption unchanged). +`requires_structure_transitional` marker (**narrow 1.1≤mass≤1.5** — excludes both the Sun and the 2 M☉ slice, else fail-not-skip) +1 test (three-layer probe rad@0.5) + structure.js caption branch. **239 pytest**, Playwright two-band verified 1440, zero errors. Next (USER choice): rotation-atlas/spectra-density threads.
