# Memory index

- [Star Simulator init scope](star-sim-init-scope.md) — what init delivered, the §3 spine, stub limits (historical: MISTProvider was the next step).
- [Star Simulator MISTProvider](star-sim-mist-provider.md) — real MIST v2.5 grids: 2D (mass×[Fe/H]) EEP interpolation, blend-then-invert, non-rectangular domain/dead corner, fetch-at-build URL discovery, empirical anchors; window widened ZAMS→EAGB (He flash/blue loop/early-AGB; cache v4; TPAGB hard-stopped — thermal pulses too chaotic to interp); full mass grid 0.1–300 M☉ + per-grid .npz parse cache (62s cold→0.35s warm). Phase 1 complete.
- [Star Simulator composition panel](star-sim-composition-panel.md) — §5.4 panel + /track endpoint: EEP-axis stacked areas, track returns list[StellarState], one endpoint also feeds the HR track, shared _state_from_row helper.
- [Star Simulator frontend UX](star-sim-frontend-ux.md) — single-source age window (track as driver, kills the /age_range race), snap-tick strips + editable inputs, hover pedagogy (? glyphs + glyph-free status tooltips via one data-tip), canvas.js fitCanvas helper, halved diagrams, start.bat launcher.
- [Star Simulator draggable/responsive panels](star-sim-draggable-responsive-panels.md) — frontend-only dashboard UX: panels movable + auto-stack to width + no overlap = a **reorder-in-flow sortable** (`layout.js`, flex-wrap `main`, grip in each h2, Pointer Events, placeholder, localStorage by `data-panel-id`, reset btn), NOT free-floating windows. **Two gotchas:** (1) `fitCanvas` sets inline canvas width → overrides CSS → canvases need JS `resize(w,h)` + ResizeObserver (the 720px spectrum/SED phone-breaker; `W/H`/`plotW` → `let`; star.js self-resizes already); (2) edge auto-scroll's `window.innerHeight` ≠ pointer `clientY` space under mobile emulation (1321 vs 844) → use `documentElement.clientHeight`. Verified mouse AND real CDP touch drag (advisor caught that mouse-only verification missed gotcha 2). pytest unchanged (137). **DENSITY-PASS FOLLOW-UP** (CSS + comp.js only, `layout.js` UNTOUCHED → touch-drag still holds): user said "a lot of empty space, doesn't look good." **Advisor: the 317px gap was ONE outlier (the 781px controls panel ≈ its 14-row readout), NOT a flex-wrap flaw → fix the readout, not the engine; and do NOT build masonry this turn** (user was brainstorming not specifying; flex-wrap already IS 2–3 cols; masonry forces re-doing CDP touch verification). Shipped: (a) **per-element legend toggle** — click a `legend-cno` entry → `comp.toggleElem`, and **the autoscale must follow** (`drawCno` passes `ELEMS.filter(!hidden)` to `region()` so hiding O/C rescales the y-axis — declutter-only is half the feature); (b) **`.readout` → `repeat(auto-fit,minmax(240px,1fr))`** = 2-col wide / 1-col phone, width-driven not media-query → controls 781→595, worst gap 317→131; (c) lane un-`wide` + `lane-body` single vertical column (kills the stranded-canvas gap); (d) spectrum/SED un-`wide` → pair on a row; wider `.panel` flex-basis 320→460/max 700. **Masonry offered as opt-in** (if built: per-column drop zones, NO auto-rebalance on drop, redistribute only on load+col-count change). Resizable panels rejected (fights responsive-flow like stored positions).
- [Star Simulator true-size scale bar](star-sim-true-size-scale-bar.md) — frontend-only UX: 3D star **+20%** (`displayRadius` → `min(2.65, 1.2*base)`; advisor caught the clip math — silhouette is the **tangent cone `asin(R/d)`** not pole `atan`, clips at R=8·sin20°≈2.74; **verify the WORST giant not the Sun**) + a **true-size scale bar** (`scale.js`, sibling like sed.js, reads `R_rsun`, no fetch): a log length axis (R☉) with **Earth/Jupiter/Sun body dots** (below) + **Mercury…Jupiter orbit rings** (above, `AU·215.032`) under a "planet orbits" bracket, a `teffToCSS` "this star · N R☉" pill (clamped on-canvas) + dynamic engulfs-caption. Honest because the 3D render is **log-compressed/not-to-scale**. Orbit cluster collides (0.6 decades) → staggered 2-row labels + collision-skip, **NO glyphs** (teaching tool); caption carries skipped figures. Wiring mirrors the RESPONSIVE/ResizeObserver `fitCanvas` pattern from [[star-sim-draggable-responsive-panels]]. Verified Playwright (dwarf/Sun/327R☉ giant, desktop+phone). pytest unchanged (137).
- [Star Simulator Phase 2 shaders](star-sim-phase2-shaders.md) — §7 beauty (frontend-only): Planck→CIE color pipeline, H_p-driven Worley granulation, quadratic limb darkening, streak-proof rotation (rigid spin + bounded-reset differential + reforming granules), activity-driven corona quad; verified via headless SwiftShader screenshots. Phase 2 complete.
- [Star Simulator Phase 3 Lane–Emden](star-sim-phase3-lane-emden.md) — §8 static polytrope panel: sibling to the §3 spine (NOT a provider), /polytrope endpoint bypasses PROVIDER, DOP853 + θ=0 event + clamped RHS, closed-form (n=0/1/5 pointwise + both invariants) & Chandrasekhar validation, ξ=100 search cap honesty fix, self-contained n-driven frontend (ρc/ρ̄ payoff). Phase 3 complete; next is Phase 4+.
- [Star Simulator Phase 4 CNO](star-sim-phase4-cno.md) — per-element composition: StellarState metals_surf/core dicts (breakdown of Z, not flat fields), MIST isotope-summed threading; C/N/O (cache v2) → +Ne/Mg/Fe (v3) → +Si/S/Ca/Ti (v5) → +Na/Al/P (v6) → +Li (v7) → +Be/F light-element panel (v8; sixteen elements, sum ~0.99 Z), state.py untouched all six times — dict design paid off. comp.js has THREE views: bulk, per-element detail (14 + linear/log toggle), and a **light view** (mode=light, Li/Be/F, log forced) — the *panel* where fragile light-element depletion shows. Gotchas: MIST diffusion → surface Fe NOT flat on the MS (Sun ×0.90), inert-tracer test at 3 M☉; Na NOT inert (Ne-Na dredge-up ×1.4 → own assertion); **Li NOT inert (depletes ×74 at 3 M☉ RGB tip) AND ~1e-10 of mass so INVISIBLE on linear → needed a LOG scale, NOT a "one-line add"**; **BORON dropped — MIST's only B isotope is radioactive b8 (~1e-83), not stable boron → panel is Li/Be/F not Be/B/F** (Be depletes ×0.35 = more robust than Li, F flat = preserved this side of AGB; fragility tracks burning T); Cr/Mn/Ni do NOT exist in MIST's network (verify the header / measure before believing an element is usable); Fe doubles as [Fe/H]-axis validator (~10^Δ); every feh=0 test short-circuits the _blend_windows path → verify off-grid; **chrome --headless --screenshot hijacks the user's running Chrome → use Playwright's bundled Chromium for headless shots**. 92 tests. Six Phase 4 element paths complete (per-element view done through Li, light panel done Li/Be/F; more single metals = dead end).
- [Star Simulator Phase 4 MESAProvider](star-sim-phase4-mesa.md) — the SECOND real provider (offline MESA history.data, different format behind same §3 boundary); discrete-grid, **multi-metallicity by snapping** (snap feh-then-mass over [Fe/H] buckets; was single-Z), true mass+feh, NO cross-mass/feh interp (no EEP in raw history = MIST's iso, out of scope). ~40-line parser, model_number dedup, ZAMS/eep/[Fe/H]/phase proxies, empty metals. fetch_mesa (bearums grid, gitignored, metal-poor Z≈0.0022). Payoff = MEASURED MESA-vs-MIST cross-validation matched on Z + central-Xc via track(): late-MS |ΔlogL|≤0.13, |ΔR|≤20%, MESA brighter/larger. Opt-in (PROVIDER stays MIST). Multi-Z **NOW REALIZED** (was latent): a near-solar [Fe/H]≈0.00 bucket (**1/2/6 M☉**) sits alongside bearums −0.84 — made by running **MESA r24.03.1 ourselves in Docker** (evbauer/mesa_lean, Z=0.0152; the MESA-Web web form silently never delivered its email → local Docker is the reliable path), drop-in needed **zero provider code**. Recipe committed at backend/docs/mesa_solar_recipe.md (output gitignored). **Solar cross-val (the payoff), masses 1/2/6, matched Z+Y+Xc:** the **1 M☉ Sun is ~10x TIGHTER** than the metal-poor bracket (|ΔlogL|≤0.014, |ΔTeff|≤0.04%, |ΔR|≤1.5%), but intermediate masses grow off ZAMS (worst Xc=0.2: |ΔlogL|≤0.069, |ΔR|≤9.9% — convective-core hook), staying inside the metal-poor envelope; **ΔlogL sign NOT uniform** (MESA brighter at 1, MIST at 2/6 → no "MESA brighter" assertion). Bracket=m050+p000 (solar ZAMS Z=0.01523 < p000's 0.01635). 102 tests. Gotchas: `pkill -x star` not `-f .../star` (self-match suicide); `docker exec -d` to detach; `pgstar_flag=.false.` skips Xming. FINDING still valid (why self-run): no fetchable native solar grid (konkolyseismolab=MIST-repackaged→circular; sai-veeresh=no surface comp).
- [Always commit and push](always-commit-push.md) — standing preference: finish any substantive change by committing AND pushing, without waiting to be asked (generalizes the session-end ritual; overrides the default git-push deny).
- [Session-end ritual](session-end-ritual.md) — at batch end / "session end": update memory + docs, commit, push (overrides the default git-push deny).
- [Star Simulator GitHub repo](star-sim-github-repo.md) — public repo location + how it was created.
- [Star Simulator non-thermal SED plan](star-sim-nonthermal-sed-plan.md) — **PLANNED, NOT BUILT**: the accepted-science answer to "model the gamma/radio floor" the blackbody SED leaves empty. Magnetic-**activity**-driven (rotation/age, not Teff/logg/[Fe/H]): cool-star rotation–activity `L_X/L_bol` (sat ~10⁻³) + Güdel–Benz `L_X/L_R≈10¹⁵·⁵Hz`; hot-star wind `L_X≈10⁻⁷` + free–free radio `S_ν∝ν^0.6` from Ṁ; flares `E⁻²`; **γ stays floored/empty**. **HONESTY GATE:** the `activity` proxy is a pure Teff ramp (no rotation) → can't draw a single L_X (the boron-b8 trap) → draw a **BAND** (saturated→quiet envelope) + the one PREDICTIVE piece, hot-star wind radio from real `star_mdot` (in _Track+cache v9, NOT yet in StellarState → Chunk 2 adds optional `mdot_msun_yr`, NO cache bump). Two epistemic tiers. User chose "band + real wind radio". Chunk 1 frontend-only (L_lsun+Teff in state); hard point = normalization onto the per-peak Fλ axis. **CHUNK 3 ADDED (synthesis, upgrade ladder band→line):** collapse the X-ray band to a line by supplying the missing rotation dim two ways — (a) AGE-DERIVED default: the sim ALREADY has age (`age_yr` = central scrubber; my "no rotation/age input" was true of the *current Teff-ramp proxy* but misleading), gyrochronology `age→P_rot→Ro→L_X/L_bol` (Barnes07/Mamajek-Hillenbrand08), frontend-only, MS-cool-stars-only; (b) activity/rotation SLIDER, user-pinned, exact precedent = Lane-Emden n-slider (auto-derive rejected as dishonest). Honest gating (✅ MS cool F/G/K; ❌ hot/no-dynamo, ❌ very-young/unconverged; ⚠️ M-dwarf-plateau, ⚠️ evolved→stop at turnoff, ⚠️ old→weakened braking van Saders16). Even pinned: ~0.5-1 dex scatter + 10× cycle → "line with a fuzz". Ladder: band→age-line→slider. Plan: `docs/plans/magnetic-ember-broadcast.md`.
- [Star Simulator WR/WD endgame plan](star-sim-wr-wd-endgame-plan.md) — **CHUNK 1 BUILT** (backend accessor+classifier; 137 tests, was 121): full Wolf–Rayet & white-dwarf endgame renderers via a reversible gateway button at the slider limits. Measured grounding (both endgames ALREADY on disk, clipped by phase>=5; WR onset feh-gated, real fine grid 35/48/56 Msun; WD cools to 27 Gyr/logg 8; star_mdot+star_mass in header; advisor caught 2 wrong guesses). Spine: NO new provider (WR/WD ARE StellarState), **snap-to-track endgame** (dissolves the TPAGB interp problem). **Chunk 1: `endgame()` on the Protocol (MESA/Stub→type="none", §3-agnostic route — NOT a hasattr-sniff), EndgameResult dataclass, snap BOTH mass+feh + report true snapped, data-derived classify (φ9→WR / φ6-or-final-logg>7→WD / φ5-onset-end→SN states=[] / else none), WR threshold scanned-not-hardcoded, star_mass/mdot→_Track+cache v8→9 but NOT mixed, StellarState untouched (no EXPECTED_KEYS change), the advisor "cooling monotonic" TRAP = only logg monotonic over post-AGB / Teff&L only past the ~107kK CSPN knee, _state_from_row eep_origin generalization reuses 16-elem metals dict.** Locked: reversible/live-mass/log-cooling-age/full-sequence. 8 chunks: **Chunk 0 (spectrum scoping) DONE** — Koester DA=GO (separate WD cube, Teff×logg, no [Fe/H]), TMAP hot WD/CSPN=conditional (SVO SSAP bulk, flux ×π×10⁸), Koester DB=NO-GO (restricted), PoWR WR=conditional (own (T*,Rt) cube, assumption-mapped v∞/D) → recipe §7; remaining 2–5 frontend on-hand data, 6–7 data-gated spectra. Also records the **hot-end finding: NO MSG grid exceeds OSTAR's 55000 K** → can't extend hot end, the no-model notice is correct. Plan: docs/plans/smoldering-cinder-gateway.md.
- [Star Simulator rotation & subpopulation atlas](star-sim-rotation-subpop-atlas.md) — **DISCUSSION CAPTURED, nothing built**: a survey of controls to show different stellar subpopulations, prompted by "rotation+age affects more than WR X-rays." User: **"nothing is out of scope"** → old §2/§9 walls (no binary engine/live solver) now recorded as candidates *with a cost*, honesty tiering kept. **Verified:** MIST v2.5 rotating grid EXISTS (`vvcrit=0.4` tarball resolves); only `vvcrit0.0` on disk; provider keys by [Fe/H] only → rotation NOT an axis yet (a 3rd axis to add); surface N (`Ns`) already plumbed → rotating tracks show **MS** N/He enrichment (Hunter diagram, not shown today); bake R≈2400 → v sin i visible only >125 km/s. **Build gate (NOT verified):** does v2.5 ramp rotation in by mass (v1.2 did, above ~1.2–1.8 M☉)? If so it does nothing at 1 M☉ → cheap diff settles it. **Atlas tiers:** A=rotation vvcrit 0.0↔0.4 (headline, 2-pt so toggle/snap; payoff MS N-enrich/lifetime/WR-threshold/CHE); B spectrum-only=[α/Fe] (CAP18-large rebake), v sin i broadening, ξ; C evocative=gravity darkening/oblateness, SED activity slider, Ap/Bp; D new-engine=**binarity** (~70% WR are binary-stripped — biggest lever), He axis, α-evolution, live solver; bonus zero-data=**instability-strip HR overlay**. Rotation+age splits by type: cool→braking/gyro (age predicts spin), hot/WR→winds+binaries (age does NOT). Cross-refs [[star-sim-nonthermal-sed-plan]], [[star-sim-wr-wd-endgame-plan]]. Plan: docs/plans/whirling-cohort-atlas.md.
- [Star Simulator roadmap (future-work index)](../plans/ROADMAP.md) — the **canonical cross-plan index** of everything proposed-but-unbuilt, one priority view spanning all four plan docs + CLAUDE.md: SED non-thermal (Chunks 1–3), WR/WD endgame (Chunks 2–7), the rotation/subpopulation atlas (Tiers A–D + instability-strip overlay bonus), and the spectra-density / BSTAR2006 / OSTAR-tier / TPAGB-pulses stragglers. Thin index (each entry = status + hook + pointer; detailed design stays in the linked doc/chunk); grouped by readiness (ready-now frontend wins → backend/data → big engines → minor/skip) with a synthesis sequencing. CLAUDE.md's "Next" now points here so the two don't drift. **Update this file (not a second list) when scope changes.**
- [Star Simulator Phase 5 spectra](star-sim-phase5-spectra.md) — synthetic-spectrum panel **DONE + CAP18 SWAP DONE (3-D [Fe/H]) + OSTAR2002 HOT SPLICE DONE (>30000 K closed, →55000 K, He lines)**: MSG/pymsg **build-time bake** (built once in `msg_spike`, gfortran 15, **no MESA SDK**) → void-filled flux-cube `data/spectra/spectra_grid.npz` (gitignored) → **pure-Python scipy runtime** `spectra.py` serving `/spectrum` (bypasses PROVIDER like `/polytrope`; `spectrum.js` a live consumer of the marker's Teff/logg/feh). First shipped **solar-only** (sg-demo, 3500–49000 K, host UNREACHABLE); **swapped to CAP18** (`sg-CAP18-coarse.h5`, 3 axes Teff/[Fe/H]/logg) → 4-D 96×12×11×2400 cube; **then SPLICED OSTAR2002** (`sg-OSTAR2002-low.h5`, TLUSTY O-star 27500–55000 K) onto the hot Teff end → **123×12×11×2400, ~76 MB, reaches 55000 K**. **Runtime+frontend stayed axis-generic through BOTH (NO BAKE_VERSION bump either time)** — the real new code is the bake going **single-grid→multi-grid** (`--hot-grid`). **CAP18 alone capped at 30000 K** (recipe's old "~50000" was WRONG); OSTAR closes that. Splice method: *append* log-spaced Teff nodes above 30000 at the cool axis' own log-step (96 cool nodes **NOT re-spread** — advisor), sample OSTAR via **`Z/Zo=10^[Fe/H]` floored at smallest positive node 0.001** (dodges a `ValueError` partial-cell cliff near OSTAR's metal-free 0.0 node), log g clamped 3.0–4.75. **Seam clean** (OSTAR/CAP18≈0.97–0.99, measured). **BSTAR2006 NOT used** (15000–30000 inside CAP18, worse seam ≈0.94; user confirmed OSTAR-only). New **same-Teff void-fill pass** stops the metal-poor+hot corner pulling a wrong-Teff cool spectrum (**6390 along-logg, 990 same-Teff, 0 fallback**). **Payoff = He I 4471/He II 4686 guides, `minTeff`-gated** (appear only when hot → He II lights up dragging into the O regime); He II deepens 0.034→0.164 over 30000→45000. Earlier advisor lessons still hold: log-spaced Teff (interp in log10 via `axis_log`), **void-fill along logg** (not 2-D NN — Teff scale swamps it), test line-depths **measured through the runtime path** not raw-pymsg, the **hot-end seam** (widen Query bounds + **clamp both ends** — why the lower CAP18 ceiling needed no code change), **CAP18 payoff test** `test_feh_axis_deepens_metal_lines` (↑[Fe/H] deepens Na D/Ca K, Balmer flat=control). **119 tests** (+`test_hot_grid_extends_above_30000_with_helium`). Verified via curl + **Playwright bundled Chromium** (chrome --headless hijacks user's Chrome; Sun→no He, 42673 K O→He II cool-blue). **HOT-END NO-MODEL NOTICE DONE:** past the 55000 K ceiling (hottest draggable ≈78500 K) the panel BLANKS + shows "No spectral model … reach {teff_max} K — this star ≈ {teff_requested} K" instead of the fake clamped spectrum; backend `spectrum_data` reports `teff_requested`/`teff_min`/`teff_max`, blank-vs-draw policy in `spectrum.js` (`teffAboveGrid()` = `teff_requested > teff_max` → `drawNoModel()`), **HOT end only** (cool floor keeps its honest small-extrap clamp → M-dwarfs/AGB tips not regressed), keyed off `teff_max` **not a literal 55000** (auto-tracks a re-bake), distinct from the 503 "not baked" state; backend clamp + `test_cool_params_clamp_to_grid_floor` untouched (advisor: keep layering honest). **120 tests** (+`test_response_reports_teff_coverage_for_no_model_notice`); Playwright on the real UI (78453 K→message, 53656 K→normal draw). **GÖTTINGEN COOL SPLICE DONE (<3500 K, the symmetric mirror of OSTAR):** the user asked "is the clamped low-T data plausible?" → MEASURED no (coolest star 2809 K, TiO bands dominate, 3500 K clamp understated them), so spliced **`sg-Goettingen-MedRes-A.h5`** (Husser+2013 PHOENIX, 1.7GB) onto the cool Teff end via `bake_spectra.py --cool-grid` (`_prepend_cool_teff`, mirror of `_append_hot_teff`). Chosen because EXACTLY 3-axis `Teff/[Fe/H]/logg`, **log [Fe/H] like CAP18 → NO Z/Zo conv**, **log g 0-6 covers dwarfs AND giants** (ruled out SPHINX = dwarf-gravity-only → would clamp cool giants). Floor 3500→**2300 K**, cube `142×12×11×2400` ~98 MB, **runtime+frontend axis-generic / NO BAKE_VERSION bump**, **≥3500 block bit-identical**. Payoff = **maxTeff-gated TiO bandhead guides** (5167/6159/7053, mirror of He II minTeff). **VO 7400 DROPPED** (reads flat — boron-b8 "verify before labeling" lesson AGAIN). Seam graceful (TiO 6159 continuous across 3500). **121 tests** (+`test_cool_grid_extends_below_3500_with_molecular_bands`). Resume/next: `docs/plans/graceful-toasting-thimble.md` + recipe `backend/docs/msg_spectra_build_recipe.md` (§5 CAP18 fetch, §5a OSTAR fetch+gotchas, §5b Göttingen cool fetch, §6 3-grid bake cmd). Build gotchas: SDK gfortran 9.2 too old (conda 15), LAPACK95 from source same gfortran, gawk-not-mawk, fresh-tree builds, explicit -I for .mod, set MESASDK_ROOT. **PLUS BROADBAND SED PANEL (gamma→radio) DONE — FRONTEND-ONLY:** user asked "wider range, gamma and radio" → only honest answer is the Planck blackbody SED (real gamma/X-ray+radio is non-thermal coronal emission, NO model grid). `sed.js` sibling like lane.js, Teff-only/no-fetch, log-log Fλ over 14 decades, 7 EM bands, Wien peak sweeps UV→vis→IR, gamma/radio FLOOR honestly (clamp like Li in comp.js). Exported color.js `planck`. NO AskUserQuestion (blackbody-vs-data isn't a real choice). pytest unchanged (frontend-only); Playwright verified, no JS errors.
